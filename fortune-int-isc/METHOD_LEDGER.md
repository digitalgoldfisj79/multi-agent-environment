# Method ledger — executed focused integer programme

This ledger prevents equivalent reformulations from being counted as new approaches.

| Lane | Final object | Required information | Execution ruling |
|---|---|---|---|
| `I1/LTQ` | one-sided count lower-tail `sum (lambda_j-Z_j)_+^2` | absolute one-failure gap | **PASSED:** strictly weaker than full variance; formal implication kernel checked |
| `I2/FM` | selected-centre first moment | restriction to increasing primorial centres at `H=(log P)^2` | **CLOSED:** not logically required after I1; no established theorem reaches the scale |
| `I3/4P` | aggregate centred four-prime covariance | signed cancellation before absolute values | **CLOSED:** raw route loses `X/L(X)` and restores the discarded upper tail |
| `I4/SOURCE` | shifted source `Psi_j=sum Lambda(P_j+m)` | one-defect lower-tail resolution after local principal-term subtraction | **PROMOTED:** reduced programme to `INT-PSLT`; theorem remains open |
| `I5/PSD` | primorial walk Gram/source-orbit formulation | cancellation after removal of smooth rational modes | **CLOSED:** exact coherence `F_X(a/q)=N` for every `q|A_X` |
| `I6/FALSIFY` | local-density and one-defect surrogate models | discriminator resolving individual failed rows | **PASSED:** first moment, relative second moment, dense averaging and local data proved insufficient |

## Terminal theorem

For `B_X=c_0 X(log X)^2`, define

\[
\mathcal D_{\Psi}^-(X)=\sum_{j<N}(B_X-\Psi_j(H))_+^2.
\]

`INT-PSLT` is

\[
\mathcal D_{\Psi}^-(X)=o(B_X^2).
\]

This is the sole active integer theorem after closeout. It has lower arity and a lower
threshold than the original full covariance architecture, but it still has the absolute
one-defect resolution required to prove Fortune.

## Excluded and closed

- Paper VII cubic geometry;
- direct function-field `d=1`;
- more pair-sum moments;
- random-order concentration;
- generic short-interval prime averages without a primorial restriction theorem;
- raw four-prime upper bounds;
- source/orbit reparametrizations that retain the smooth-modulus coherent modes;
- finite scans used to claim uniform asymptotics.

An excluded method can re-enter only with a committed theorem mapping it to `INT-PSLT`
and a complete exponent audit.
