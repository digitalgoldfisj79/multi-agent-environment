name: Papers II and III reconstruction trigger

on:
  workflow_dispatch:

# The pull-request reconstruction logic is frozen on the PR base branch.
jobs:
  marker:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Reconstruction is executed from the base-branch workflow.'
