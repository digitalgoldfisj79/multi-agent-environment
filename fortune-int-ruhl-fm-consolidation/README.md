# Fortune INT-RUHL-FM consolidation

**Programme:** `FORTUNE_INT_RUHL_FM_CONSOLIDATION_V0_1`  
**Date:** 6 August 2026  
**Branch:** `gpt56/fortune-ruhl-fm-consolidation-v01-20260806`  
**Base:** `560f6fc5f5723db96dc43b1a7d1a361fc14e9c3d`  
**Status:** active conditional-theorem consolidation

## Purpose

This programme executes the review-corrected handoff in `fortune-int-scme-selected-centre-mean/SUCCESSOR.md`.

It consolidates the existing adaptive occupancy detector, even Bonferroni truncation, factorial-moment hypothesis, cumulant alternative and obstruction records into:

1. one exact conditional theorem;
2. one explicit arithmetic error interface;
3. one honest frontier map.

It does **not** reopen `INT-SCPT`, assert that exact phase control is the sole remaining obstruction, or claim an unconditional proof of `INT-AOD` or Fortune's conjecture.

## Current mathematical result

The inherited `RUHL-FM` implication is correct. The consolidation sharpens it in three ways:

- the logically minimal input is a one-sided signed Bonferroni error bound;
- the sufficient absolute-error budget is the exact remaining detector margin, asymptotic to `1/(n_b B)`, rather than the stronger registered budget `(n_b B)^(-1-2 epsilon)`;
- under `epsilon=0.10` and `U_b/L_b<=1.10`, the factorial truncation constant may be reduced from the exhibited `beta=10` to `beta=5`.

These are conditional-architecture improvements. They do not supply the missing prime-tuple estimates on the selected primorial path.

## Files

- `PROGRAMME.md` — preregistered gates and stop rules;
- `THEOREM_RUHL_FM.md` — exact theorem and proof;
- `ARITHMETIC_INTERFACE.md` — decomposition of the factorial-moment error into prime-tuple and model-collapse terms;
- `FRONTIER_MAP.md` — relationship to `INT-SCME`, `INT-LCSK`, `INT-PWOC`, `INT-SOCG` and `INT-AOD`;
- `scripts/verify_ruhl_budget.py` — deterministic parameter certificate;
- `STATUS.md` — execution ledger and next target.
