# RUHL selected-tuple residual programme

This package executes one direct Fortune programme against the validated implication

\[
\mathrm{RUHL\!\!-\!FM}\Rightarrow\mathrm{INT\!\!-\!AOD}
\Rightarrow\text{eventual Fortune}.
\]

The programme establishes that the signed hypothesis is the truncated detector discrepancy itself, while the independent absolute hypothesis already demands additive constant accuracy for the selected-centre first moment. It then audits one exact Heath--Brown identity and proves the source-scale/absolute-coefficient dichotomy preventing the registered implementation from reaching the detector margin.

Run:

```bash
python fortune-ruhl-selected-tuple-residual/scripts/verify_programme.py
```

Expected sentinel:

`FORTUNE_RUHL_SELECTED_TUPLE_RESIDUAL_EXACT_PASS`
