# R0 — direct Fortune chain

The only active implication is

\[
\boxed{
\mathrm{RUHL\!\!-\!FM}\Longrightarrow\mathrm{INT\!\!-\!AOD}
\Longrightarrow\text{eventual Fortune}.
}
\]

The finite even-Bonferroni theorem and adaptive detector implication are inherited and already validated. This programme does not attempt to prove `INT-SCME`, `INT-LCSK`, or `INT-PWOC` separately. Those remain alternative component routes, not active workstreams.

A result counts as progress only if it bounds the exact RUHL residual or reduces it to one explicit estimate whose proof would immediately satisfy the registered detector margin.
