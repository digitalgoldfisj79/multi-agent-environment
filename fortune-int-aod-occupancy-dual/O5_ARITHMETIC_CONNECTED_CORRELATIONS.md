# O5 — arithmetic connected correlations

For a deterministic stratum and candidate column `m`, let

\[
I_m(J)=1_{\mathbb P}(m)1_{\mathbb P}(P_J+m),
\qquad Z_J=\sum_m I_m(J).
\]

The ordinary cumulants of `Z_J` have the exact multilinear expansion

\[
c_{k,b}=\sum_{m_1,\ldots,m_k}
\operatorname{Cum}_b(I_{m_1},\ldots,I_{m_k}),
\]

where ordered tuples include repetitions and

\[
\operatorname{Cum}_b(I_{m_1},\ldots,I_{m_k})
=\sum_{\pi\in\Pi_k}(|\pi|-1)!(-1)^{|\pi|-1}
\prod_{C\in\pi}\mathbb E_b\prod_{r\in C}I_{m_r}.
\]

This is the common-row connected quantity relevant to

\[
\log\mathbb E_b e^{-\tau Z_J}.
\]

The initially proposed distinct-column factorial-cumulant identity was rejected by exact validation and is not used.

## Arithmetic task

Disconnected selected-centre prime-tuple terms must cancel at the cumulant level before absolute estimates are applied. The local-factor audit separates primes dividing the primorial, collision primes from offset differences, repeated-offset identifications, and the tail Euler product.

## Executed outcome

The safe dependency graph is complete and generic absolute cluster criteria fail at weighted activity `Theta(log X)`. The smallest retained target is `INT-SOCG`, the stratified ordinary-cumulant growth theorem stated in `O5_EXECUTION.md`.
