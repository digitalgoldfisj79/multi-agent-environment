# Successor target — INT-SOCG

`INT-SOCG` is the sole primary integer frontier after the O0–O9 closeout.

For a deterministic polylogarithmic partition of registered primorial rows, prove preregistered lower mean scales `L_b>=cX`, temperatures

\[
\tau_b=(1+3\varepsilon)\log(n_bB)/L_b\le\tau_A,
\]

and ordinary-cumulant dependence scales

\[
D_b\ll X/(\log X)^{1+\delta}
\]

such that

\[
c_{1,b}\ge L_b,
\qquad
|c_{k,b}|\le c_{1,b}k!D_b^{k-1}\quad(k\ge2).
\]

This implies the stratified detector, `INT-AOD`, and eventual Fortune. The theorem is open.
