# C4 — connected local-factor renormalization

For a fully distinct offset tuple, the joint moments carry a primorial singular-series enhancement from primes at most `ell_j`, collision factors from primes dividing offset differences, and a tail Euler product.

## Renormalization order

1. remove the universal small-prime primorial factor once at the joint-moment level;
2. apply partition Möbius recombination;
3. only then estimate the connected residual;
4. keep collision primes and tail factors separate.

Define the renormalized connected local kernel `K_conn(m_1,...,m_r)`.

## Candidate theorem — INT-LCSK

Prove a tree or forest majorant whose aggregate radius `D_local` satisfies

\[
D_{local}\ll X/(\log X)^{1+\delta}.
\]

## Kill gate

If absolute local interactions alone force `D_local\gg X/log X`, close the connected-singular-series lane before any primorial-walk or source computation.