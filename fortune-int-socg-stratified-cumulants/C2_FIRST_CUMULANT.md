# C2 — first-cumulant lower bound

The first independent boulder is

\[
c_{1,b}=\frac1{n_b}\sum_{j\in B_b}
\sum_{\ell_j<m\le H}1_{\mathbb P}(m)1_{\mathbb P}(P_j+m)
\ge cX.
\]

## Required decomposition

1. derive the exact von Mangoldt-weighted selected-centre mean;
2. separate candidate-prime weights from output-prime weights;
3. bound proper prime powers at the absolute stratum scale;
4. state the singular-series main term without silently transferring an ordinary moving-centre theorem;
5. identify the smallest error estimate that implies the unweighted lower bound.

## Permitted outcomes

- prove the lower bound;
- reduce it to one named selected-centre mean theorem with all ranges explicit;
- record `MEAN_LOWER_BOUND_IS_PRIMARY_OBSTRUCTION` if no smaller higher-cumulant campaign is rational before this theorem.

## Prohibited shortcut

`c_{1,b}\asymp X` may not be imported as a heuristic or inferred from finite panels.