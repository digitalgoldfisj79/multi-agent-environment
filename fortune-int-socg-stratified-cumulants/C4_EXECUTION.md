# C4 execution — local connected kernel

**Status:** `PAIR_SCALE_PASSED; ALL_ORDERS_TREE_MAJORANT_OPEN`

After removing the universal primorial small-prime enhancement once at factorial-moment order `r`, the remaining local interaction is supported on primes above the terminal boundary.

For two distinct offsets define the edge majorant

\[
\beta_X(d)=
C\sum_{\substack{p>2X\\p\mid d}}\frac1p
+
C\sum_{p>2X}\frac1{p^2}.
\]

The first term records collision primes; the second dominates the generic Euler-product tail when residues are distinct. For `r=O(log X)` and `p>2X`, expansion of

\[
\frac{1-\nu_p(\mathbf m)/p}{(1-1/p)^r}
\]

shows that every local connected term is generated by collision contributions of size `O(1/p)` and generic pair interactions of size `O(1/p^2)`.

For the candidate-prime column set `M_b`, Brun--Titchmarsh in the progressions `n congruent m (mod p)`, dyadic summation over `p>2X`, and

\[
\sum_{p>y}p^{-2}\ll(y\log y)^{-1}
\]

give the row-sum scale

\[
\boxed{
\sup_{m\in\mathcal M_b}
\sum_{\substack{n\in\mathcal M_b\\n\ne m}}
\beta_X(m-n)
\ll\frac{X}{(\log X)^2}.
}
\]

Thus pairwise absolute local interaction is already below the target `X/(log X)^(1+delta)` for every fixed `delta<1`. The transition primes inside one deterministic stratum contribute only `O((log X)^(-5/2))` reciprocal mass.

Finite diagnostics through `X=150` found maximum edge row-sums below `0.20 X/(log X)^2`; these values are diagnostic only.

What remains open is the uniform all-orders connected-hypergraph/tree majorant that converts the primewise local expansion into factorial-cumulant bounds through logarithmic order. This precise residual is `INT-LCSK`. C4 finds no local pair-scale obstruction but does not claim `INT-LCSK`.
