# Standalone Paper III — claim status

**Publication role:** intrinsic superincreasing pair-sum kernel theory plus an independently derived primorial-centre covariance application.

## Proved in the manuscript

- bounded-coefficient rigidity;
- linkage and exact represented-difference multiplicity dichotomy;
- exact two-scale energy decomposition;
- high-moment bound and full-range sub-Weibull tail under the stated length condition;
- exact fourth moment and centred second moment derived locally from the multiplicity dichotomy;
- quantified sparse-sampling transfer-gap corollary;
- candidate collapse at primorial centres;
- deterministic all-centres variance implication;
- exact four-prime second-moment expansion.

## Exact finite combinatorial evidence, not a downstream proof input

- the sixth-moment and centred third-moment polynomial identities in (7.4)--(7.5), reproduced by exhaustive endpoint-partition enumeration.

## Conjectural calibration only

- the Hardy--Littlewood baseline (9.4).

## Open

- the sparse reciprocal non-concentration estimate (8.3);
- the primorial-centre variance estimate (9.6);
- sufficient aggregate four-prime covariance estimates such as (10.3)--(10.4);
- Fortune's conjecture.

## Source repair

The corrected source main text stated in Theorem 3.1 that every non-single-walk nonzero integer difference has `r(D)=1`. This is too strong because an unrepresented integer has `r(D)=0`. The fuller frozen Appendix A.3 already contains the correct statement `r(D)<=1`; the standalone theorem follows that formulation and proves that every *represented* difference outside the sliding family has multiplicity one.
