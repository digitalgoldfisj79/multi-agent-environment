# Programme — FORTUNE_INT_SCME_SELECTED_CENTRE_MEAN_V0_1

## Archived objective

The programme tested whether the selected-centre weighted mean `INT-SCME` could be proved or reduced using deterministic microblocks, selected-residue variance and a post-terminal divisor band.

## Review-corrected outcome

The unconditional selected-residue variance route is obstructed at an explicit exponent scale. The conditional band calculation is valid under `INT-SCVAR`, but subtracting that composite band leaves a remainder inequality equivalent to `INT-SCME`; it is not a new parity-breaking target.

The programme also does not reduce `INT-SOCG` to `INT-SCME`. The full theorem still requires the all-orders `INT-LCSK` and composite-modulus `INT-PWOC` inputs.

## Durable outputs

1. collision-energy theorem;
2. conditional exponent frontier;
3. BDH range correction;
4. direct asymptotic-sieve scale gap;
5. unconditional large-sieve obstruction;
6. exact first-cumulant implication;
7. an explicit record of the false high-level reductions and their correction.

## Next-work prohibition

No successor programme may attack `INT-SCPT` as if it were distinct from `INT-SCME`, or describe `INT-SCME` as sufficient for `INT-SOCG`.
