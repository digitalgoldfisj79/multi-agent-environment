# M0 — source freeze

**Status:** `PASSED`

Frozen inputs:

- issue #58, `INT-SCME: selected-centre weighted mean`;
- PR #57 head `e1a9822e8f43c37329c03a863595af522626d73b`;
- deterministic parent strata of width `X/(log X)^(3/2)`;
- common candidate-prime universe `M_b={m:U_b<m<=H, m prime}`;
- `H=X^2/2`;
- the inherited output-prime-power bound and `INT-SCME => c_1 >= cX` implication;
- the prime-modulus primorial collision theorem from the INT-SOCG closeout.

No output-prime incidence, occupancy count or fitted temperature is used to define a parent stratum or microblock.