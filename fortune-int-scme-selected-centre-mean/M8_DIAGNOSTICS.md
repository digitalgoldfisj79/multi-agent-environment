# M8 — exact diagnostics and controls

**Status:** `READY`

The execution suite must verify:

- deterministic microblock aggregation;
- exact collision multiplicity identities on finite primorial walks;
- the exponent conditions `2 delta < rho < 1-delta`;
- optimality of `delta<1/3` inside the registered BDH/Cauchy mechanism;
- the exact decomposition `Lambda=D_Q+R_Q`;
- finite local-density products;
- small factor-profile panels separating prime outputs, band-divisible composites and composites with no detected band factor.

Controls:

- replacing outputs by an adversarial all-composite panel must leave the divisor-band statistic positive while destroying prime mass;
- no diagnostic may infer `INT-SCPT` from positivity of `D_Q`;
- factorization panels are restricted to small `X` and have no asymptotic status.