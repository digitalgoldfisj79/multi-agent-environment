# M9 — closeout

**Status:** `PENDING_VALIDATION`

The registered terminal candidate is

`METHOD_OBSTRUCTED_AT_EXPLICIT_SCALE`.

The exact conditional anatomy retained is

\[
INT\text{-}SCVAR+INT\text{-}SCPT
\Longrightarrow INT\text{-}SCME.
\]

Closeout requires:

1. all corrected exact Python regressions, including the unconditional large-sieve obstruction;
2. selected finite diagnostics with no promotion;
3. inherited `INT-SOCG`, `INT-AOD`, `INT-PFLI`, factor-band and mainline verifiers;
4. targeted Lean for the exact band-plus-tail implication;
5. full Lean 4.32 package build and formal trust audit;
6. final branch re-clone after all issue and PR writes;
7. no active programme compute job.

The initial unconditional M5 claim is excluded from evidence. Every accepted validation job must postdate commit `12c58e1c6d45c8fd5ee88269e2aa2a7dc91c8113` or a later correction commit.