# d=1 crown push — Phase Z6 Adams localization status

**Date:** 2026-07-23  
**Branch:** `gpt56/d1-push-weight0-collapse-20260722`  
**Status:** the Adams/cyclic bridge, the weight-three exponent, the two-degree cohomological concentration, and the complete local boundary inventory are now exact. The general function-field `d=1` crown remains open because a uniform effective Betti bound for the global primitive gluing complex has not been proved.

## 1. Executive result

Phase Z6 produced a genuine narrowing.

The exact depressed-slice error

`A_r(a)=pN_a(p^r)-p^(2r)`

is no longer merely observed to have weight-three scale. It is proved to be the Frobenius trace of a virtual complex of weights at most three. After affine Artin vanishing, that normalized complex is concentrated only in compactly supported degrees `2` and `3`.

Moreover, every local boundary type of the weighted compactification is explicit:

1. every finite branch and collision is annihilated by the Adams defect;
2. the finite part of wild infinity is formally constant;
3. the weighted corner is a tame deformation of an Artin–Schreier family;
4. one endpoint is tame of cycle type `(p-1,1)` and Adams-annihilated;
5. the other endpoint is exactly Artin–Schreier infinity.

Thus the missing theorem is not another local classification and not another weight estimate. It is a global effective-degree bound for the residual gluing class after all explicit summands are removed.

## 2. General Moore and root-negation theorems

For `K=F_(p^p)`, `u!=0`, `v=u^p`, `w=u^(p^2)`, define

`Xi_a(u)`

`=[v^2-uw-auv(2u^2+3uv+v^2)]/[3auv(u+v)].`

The Moore determinant becomes linear after putting `u=x^p-x`, giving the exact theorem

`pN_a(p)=#{u in K^*:Xi_a(u)^p-Xi_a(u)=u}.`

Separately, with

`H_c(Y)=Y^((p-1)/2)+aY+c`,

`G_(c,e)(Y)=YH_c(Y)^2-e`,

one has

`G_(c,d^2)(X^2)=F_(c,d)(X)F_(c,-d)(X)`

and, for `d!=0`,

`F_(c,d) irreducible iff G_(c,d^2) irreducible.`

Both are now proved for every prime `p>=5`; the combined symbolic/factorization audit passed through `p=59` for both square classes.

## 3. Cyclic and Adams compression

Let `P=1 direct_sum V` be the degree-`p` permutation representation of `S_p`, let `C_p` be generated by a p-cycle, and let `psi` be a nontrivial character of `C_p`.

The complete alternating hook object satisfies the exact identities

`lambda_(-1)(V)`

`=Ind_(C_p)^(S_p)1-Ind_(C_p)^(S_p)psi`

`=Psi^p(P)-P.`

Its character is `p` on the p-cycle class and zero elsewhere. Hence the all-degree irreducibility sieve is one Adams defect, not a sequence of independent factor-degree conditions.

On the original depressed cover, with root permutation sheaf `P_a` and `W_a=Psi^p(P_a)-P_a`, one has over every `q=p^r`:

`boxed(pN_a(q)=Tr(Frob_q|RGamma_c(U_a,W_a)).)`

## 4. Unique top-weight extraction

The Adams hook class contains the trivial representation exactly once. The sign representation also occurs once but has no geometric `S_p` invariants.

Poincare duality on the smooth connected surface gives

`H_c^4(U_(a,bar F_p),W_a)=Q_l(-2)`

in the Grothendieck group. Therefore its trace is exactly `q^2`, with no additional weight-four term.

After removing this class,

`C_a=RGamma_c(U_a,W_a)-Q_l(-2)[-4]`

has weights at most three and

`boxed(pN_a(p^r)-p^(2r)=Tr(Frob_p^r|C_a).)`

This proves the exponent `3/2`. The exact symmetric-group audit checked all `267,863` conjugacy types for every prime through `47`; the p-cycle identity and the trivial/sign multiplicities passed in every case.

## 5. Two-degree concentration

Because `U_a` is a smooth affine surface, every lisse constituent shifted by `[2]` is perverse. Affine Artin vanishing gives

`H_c^i(U_a,W_a)=0` for `i<2`.

After removing the unique degree-4 Tate class, only degrees `2` and `3` remain:

`pN_a(p^r)-p^(2r)`

`=Tr(Frob_p^r|H_c^2(U_a,W_a))`

` -Tr(Frob_p^r|H_c^3(U_a,W_a)).`

The first has weights at most `2`; the second has weights at most `3`. Thus

`|pN_a(p)-p^2|<=b_2^eff(p,a)p+b_3^eff(p,a)p^(3/2).`

The terminal task is to prove absolute bounds for the effective primitive degrees `b_2^eff,b_3^eff` after subtracting all explicit classes.

## 6. Complete finite-boundary annihilation

For the descended cover

`R_a(c,Y)=Y(Y^((p-1)/2)+aY+c)=0`

with the square applied as in the exact theorem, the finite discriminant has only `e=0` and `e=B_a(c)`.

Generic finite inertia consists of transpositions or products of disjoint transpositions. At their collisions the multiplicity partitions are

- `3,2^((p-3)/2)` at `c=0`;
- `1,4,2^((p-5)/2)` at every nonzero collision.

No inertia element is a p-cycle. Therefore the Adams defect restricts to zero at every finite boundary point, including the full collision scheme, despite its growing algebraic degree.

## 7. Infinity and weighted corner

At infinity the local inertia group is

`C_p semidirect C_((p-1)/2)`

and the Adams restriction is exactly

`W|I_infinity=-V+2Q`,

with `Q` the inflated regular representation of the tame quotient.

The formal cover along every finite `c` on the infinity divisor is independent of `c` after a unique even formal coordinate change. Hence only the corner `c=e=infinity` varies.

The weighted blow-up has strict-transform equation

`Rz^p-Sz^(p-1)-RS-a u^(p-3)RSz^(p-3)=0.`

The normal exponent `p-3` is tame. On the exceptional divisor it becomes

`Rz^p-Sz^(p-1)-RS=0`,

and on `RSz!=0`, with `x=1/z`,

`x^p+R^(-1)x-S^(-1)=0`.

This is a tame Kummer form of the universal Artin–Schreier family. Its open exceptional contribution is elementary Tate/Artin–Schreier, not a new primitive motive.

The two endpoints are also exact:

1. `R=0`: local type `(p-1,1)`, Adams value zero;
2. `S=0`: `b=x^p+x`, the infinity point of the same Artin–Schreier family.

All weighted-corner, Artin–Schreier and endpoint audits passed for every prime through `101` after correcting one audit-only overall-sign convention in the `S` chart.

## 8. Extension-field diagnostics

New exact `r=2` counts were completed at `p=17,19,23,29,31` for both square classes. They provide the second local coefficient

`e_2=(A_1^2-A_2)/2`.

The signs and magnitudes vary substantially; for example:

- `p=17`: `e_2=51,1207`;
- `p=23`: `e_2=36731,4991`;
- `p=31`: `e_2=-1519,21545`.

This rejects a uniform honest rank-one description. The data remain compatible with the now-proved virtual weight-at-most-three complex. Since the weight exponent is a theorem, deeper extension-field enumeration has low marginal value.

## 9. Weight-four modular fingerprint

A bounded scan tested `283` rational weight-four newforms at every level `N<=150`, the square/nonsquare middle residuals, their sum and difference, and `18` small quadratic twists.

There were no exact matches. The best fit had only `R^2=0.553627...`.

Thus the residual is not one rational weight-four newform of level at most `150`, up to the tested twists and scalar multiples. This does not exclude a sum of forms, non-rational coefficient fields, higher levels, or a higher-rank geometric complex.

## 10. The exact remaining theorem

Let `C_a^prim` denote the degree-`2/3` Adams complex after removing:

- the unique top Tate class;
- the elementary main and Artin–Schreier classes;
- the Kummer sector;
- the pair sector;
- the split and nonsplit `D`/CM sectors;
- the explicit finite and infinity boundary classes.

The only required theorem is now:

### Primitive effective-degree lemma

There exist explicit absolute constants `C_2,C_3` such that for every prime `p`, and at least one square class `a`,

`b_2^eff(C_a^prim)<=C_2`,

`b_3^eff(C_a^prim)<=C_3`.

Then

`|pN_a(p)-p^2|<=C_2p+C_3p^(3/2)+O(p)`,

so `N_a(p)>0` for all sufficiently large `p`; the existing exact counter closes the finite remainder.

## 11. Recommended proof mechanism

The next phase should not expand the Adams class back into hooks.

It should:

1. build the global localization triangle on the weighted compactification;
2. identify the primitive virtual class after the explicit summands are removed;
3. compute its singular support and characteristic-cycle multiplicities;
4. use a generic pencil and vanishing-cycle dimensions to bound `H_c^3` effectively;
5. control `H_c^2` from the same localization triangle and endpoint attachments.

A characteristic-cycle Euler number alone is insufficient because the class is virtual and may have cancellation between degrees `2` and `3`. The required calculation must control effective vanishing-cycle degrees, not only their alternating Euler characteristic.

## 12. Epistemic classification

### Exact general theorems

- Moore–Artin–Schreier reduction;
- root-negation descent;
- cyclic-resolvent and Adams identities;
- exact compactly supported count formula;
- unique top Tate class and weight-at-most-three normalization;
- concentration in degrees `2` and `3`;
- complete finite-inertia annihilation;
- formal infinity rigidity;
- weighted Artin–Schreier corner and both endpoint types.

### Exact finite computation

- combined reduction audit through `p=59`;
- Adams character audit through `p=47`;
- corner and endpoint audits through `p=101`;
- extension-field traces through `r=2` at `p=17,19,23,29,31`, in addition to the earlier `p=5,7,11,13` data;
- bounded newform scan described above;
- all previous Phase Z4/Z5 exact ledgers.

### Open

- global derived gluing of the primitive boundary class;
- absolute effective-degree bounds in degrees `2` and `3`;
- the resulting explicit `N_a=p+O(sqrt p)` theorem;
- the function-field `d=1` crown;
- the integer Fortune conjecture.
