# Main d=1 status after the relative configuration-space obstruction

**Date:** 2026-07-25  
**Branch:** `gpt56/d1-main-twisted-descent-20260724`  
**Scope:** function-field `d=1` Fortune sibling only. Papers V and VI remain frozen.

## Ruling

The function-field crown remains open.

The relative configuration-space phase specified in the handover has been completed and closed by a theorem-level obstruction.

The canonical ordered-configuration face complex exists and is exact:

\[
0\to\bigwedge^p\operatorname{Perm}
\to\cdots\to
\bigwedge^1\operatorname{Perm}
\to\mathbf1
\to0.
\]

Its Euler class is zero, not the irreducibility hook class

\[
\lambda_{-1}(\operatorname{Std})
=
\sum_i(-1)^i\bigwedge^i\operatorname{Std}.
\]

Any complex of semisimple generic `S_p` local systems with this hook Euler class must retain at least one copy of every hook and therefore has total generic homology rank at least

\[
2^{p-1}.
\]

Moreover, canonical deletion/insertion correspondences are `S_p`-equivariant and preserve hook labels. They cannot create a differential between adjacent hooks after parabolic pushforward.

Therefore the requested relative configuration-space face-map mechanism cannot produce the desired `O(p)` effective middle survivor.

Authoritative theorem:

- `RELATIVE_CONFIGURATION_COMPLEX_OBSTRUCTION_20260725.md`

Verifier:

- `relative_configuration_complex_verify.py`

## Exact additional theorem: virtual middle rank

The local monodromy and proved end pieces determine the signed parabolic rank:

\[
\sum_{i=0}^{p-1}(-1)^i
\dim H^1(\mathbf P^1,j_*V_i)
=-(p-3).
\]

After removing `V_1`, `V_2`, `V_(p-2)`, and `V_(p-1)`, the middle object has exact virtual rank

\[
\boxed{
\operatorname{vrank}\mathcal M_q
=
p-3-2\left\lfloor\frac{p-1}{4}\right\rfloor.
}
\]

Thus:

\[
p=5:0,\qquad p=7:2,\qquad p=11:4.
\]

This is a signed rank theorem only. It does not imply semisimplified Frobenius cancellation.

## Regression results

The canonical face complex was tested against all three mandatory regressions.

1. **Weight-zero Kummer theorem:** fail. The exact face complex gives zero, not the Kummer line.
2. **Complete `p=5` decomposition:** fail. The exact face complex gives zero, not `K_q+H^1(B_q)-H^1(D_q)`.
3. **`p=7` middle block:** fail. `V_3` and `V_4` are nonisomorphic irreducibles, so canonical `S_7`-equivariant face maps cannot pair them.

The candidate is therefore rejected exactly as required by Phase 2.

## Closed route

Closed:

- relative ordered-configuration complexes using canonical deletion/insertion face maps;
- any claim that such a complex is exact on the generic interior while its Euler class is the hook detector;
- any attempt to obtain the post-pushforward adjacent-hook differential solely from `S_p`-equivariant configuration incidence maps.

This does not close all geometric approaches.

## Surviving main-branch routes

The next viable mechanism must not be a relative face differential. The live options are:

1. the global `S_p`-equivariant Fourier--Cayley decomposition of the sparse power-sum section;
2. direct computation or bounding of the invariant/quadratic q-line projectors;
3. a Frobenius/Adams correspondence whose operation is not generated by the canonical configuration face algebra.

The global Fourier--Cayley route is now the most structurally aligned continuation because it can compare the sparse hook object to the ambient Airy constituent without asserting generic exactness of a hook-Euler complex.

## Stopping condition

The handover instructed the programme to stop on a theorem-level obstruction closing the configuration-space route.

That stopping condition has been reached.
