# d=1 crown push — Phase Z4 four-route big-compute programme

**Date:** 2026-07-23
**Branch:** `gpt56/d1-push-weight0-collapse-20260722`
**Status:** frozen execution programme after the exact refutation of the complete Cartier support cutoff.

## Objective

Run the four scientifically live routes in parallel. Computation is used to locate exact finite patterns, refute false uniform claims, identify small motives or recurrences, and produce certified witnesses. Finite data are not promoted to general-prime theorems.

## Track Q — quantized residue conversion

Compute exact depressed-slice irreducible counts

`N_a(p)=#{(c,d):X^p+aX^3+cX+d irreducible}`

for larger stress-test primes and both square classes using the independent sparse Rabin implementation. Record `N_a/p`, `N_a mod p`, positivity and the sharp `N_a<2p` gate. Immediate adversarial primes: 401 and 503, with continuation if runtime permits.

**Pass signal:** a new exact structural congruence, a residue zero with positive count, or a significantly sharper uniform-looking envelope.

**Fail signal:** a count reaches or exceeds `2p`, refuting the current quantization gate.

## Track G — geometric direct-image pairing

Use exact cycle-type censuses over extension fields for the normalized root cover. For the first unresolved upper hook `V_(p-3)=V_2 tensor sign`, compute q-averaged extension traces and Berlekamp–Massey recurrence degrees at p=11,13,17. This fingerprints the rank growth of the primitive complement after the known sign pairing.

**Pass signal:** bounded or linear-rank recurrence compatible with an explicit small-factor decomposition.

**Negative signal:** recurrence degree growing quadratically or failure of all small-factor models.

## Track M — fixed-dimensional motives and all-degree sieve

Extend exact cubic pair/mixed moments to substantially larger primes. Compute centered residuals and compare them against a library of small elliptic-curve Frobenius traces and twists. Use train/holdout fitting; accept only exact or stable low-height decompositions.

**Pass signal:** an exact small-motive decomposition or a uniform effective residual constant strong enough for the signed sieve.

**Negative signal:** no low-complexity elliptic decomposition and residual constants too large for the current sieve.

## Track D — direct dynamics

Search a fixed low-height library of cubic maps `g` and test irreducibility of `T^p-g(T)`, equivalently existence of an exact g-composition-period-p point. Evaluate all odd primes below 1500, compute coverage of every fixed map, and find a greedy finite menu. Search for congruence-class rules selecting one map.

**Pass signal:** one universal map, or a small residue-class menu with no failures and a plausible algebraic explanation.

**Negative signal:** unavoidable uncovered primes or a menu size growing materially with the range.

## Evidence discipline

Each track must preserve source, exact command, hardware/job ID, raw or reproducible result data, and an epistemic status note. No route is declared proved from finite computation. The function-field d=1 crown and integer Fortune conjecture remain open unless a general-prime argument is obtained.