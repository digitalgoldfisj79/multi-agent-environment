{
 "best_next": "The wild-inertia input and Wall B ledger are now double-verified; the open door remains the parity-reversing pairing. The most promising step is to exploit the newly noticed arithmetic/geometric discrepancy at infinity: the disc unit-class computation (disc = nonresidue * s^{2p-4} for some (a,c)) shows the arithmetic monodromy at infinity is strictly larger than the geometric inertia by an unramified quadratic twist that depends on (a,c) through chi(3a)-type data \u2014 compute the exact arithmetic Frobenius structure on the two q-families (epsilon = +1/-1) at infinity, since a quadratic-twist mismatch between even and odd hook pushforwards is precisely the kind of O(p)-defect object the parity-reversing correspondence needs to produce, and it is finitely computable per p.",
 "summary": "Both soft spots close as CONFIRMED. (1) Wall B: independent re-derivation of the hook cohomology ledger \u2014 with C_p-invariants computed by brute-force zero-sum subset enumeration rather than the closed form \u2014 reproduces the branch script row-for-row at p=5,7,11,13 (totals 23/101/1769/7247; even-odd = 4-p; total = ((2p-3)2^{p-1}+3)/p; alternating Swan = -(p-3)); Swan integrality and the break-decomposition derivation check out. (2) WILD_INFINITY_INERTIA.md: all claims verified by direct local computation at p=5,7 over ALL parameters (a != 0, all c; all valid q): different exponent 2p-4 with lowest coefficient exactly 3a; single-segment ramification polygon of slope -(p-3)/(p-1); m=(p-1)/2 (not p-1) confirmed via the geometric discriminant-square criterion plus an independent tame-uniqueness argument over K^ur; jump j=(p-3)/2 (single jump automatic since the wild group has prime order p); tame transposition monodromy at t=+1 (double point exactly at z=1) and t=-1 (at z=-1) for every q != 0,2; Lambda_p restriction character, dim invariants = 2, virtual Swan -(p-3) all confirmed on the explicit group C_p x| C_{(p-1)/2}. One subtlety worth recording: the ARITHMETIC Galois group over F_p((s)) can exceed the inertia by an unramified quadratic (e.g. p=5, a=1, c=0: disc = 3 s^6, nonresidue unit class) \u2014 the doc's I_infty claim is about geometric inertia and stands, but a naive disc-square test over F_p((s)) would falsely refute it.",
 "key_claims": [
  {
   "claim": "HCE.1: dim V_i^{C_p} = (C(p-1,i) + (p-1)(-1)^i)/p equals the number of i-subsets of F_p^* with zero sum",
   "status": "proved",
   "notes": "Verified by exhaustive subset enumeration (independent of the closed form) for all 0<=i<=p-1, p=5,7,11,13; integrality holds in every case. The character-average derivation ((1+t)^{p-1} vs (1+t^p)/(1+t)) is standard and exact."
  },
  {
   "claim": "HCE.2: Swan_infty(V_i) = ((p-3)/(p-1))(rank - invariants), an integer for every i",
   "status": "proved",
   "notes": "Formula is the standard single-break Swan computation Swan = j*(|C_p|/|I|)*codim V^{C_p} = (j/m)*codim with j=(p-3)/2, m=(p-1)/2; integrality verified exactly for all i, p=5,7,11,13; alternating sum = -(p-3) verified."
  },
  {
   "claim": "HCE.3/HCE.4: h^1_c = rank + Swan + [i=0]; totals 23, 101, 1769, 7247 for p=5,7,11,13; total = ((2p-3)2^{p-1}+3)/p; even-odd = 4-p; even=(T+4-p)/2, odd=(T-4+p)/2",
   "status": "proved",
   "notes": "GOS on the 3-punctured line (chi_c(U)=-1, tame at +-1 so no finite Swan terms), h^0_c=0 on an affine curve, h^2_c = coinvariants = [i=0] since global geometric monodromy is S_p (transitive + transposition + p-cycle from I_infty) and the hooks V_i, 1<=i<=p-1, are nontrivial irreducibles. Independent code matches the branch script hook_cohomology_effectivity_ledger.py and the stored results JSON row-for-row and total-for-total at p=5,7,11,13. Conditional only on the wild-inertia input, which is itself verified below."
  },
  {
   "claim": "WII.1: different exponent d(E/K) = v_y(ds/dy) = 2p-4 at infinity, uniformly for a != 0 (any c)",
   "status": "proved",
   "notes": "Symbolic computation in char p at p=5,7: the numerator of ds/dy has lowest term exactly 3a*y^{2p-4} against a unit denominator; nonzero iff a != 0 (p>3). Also confirmed via v_s(disc F) = 2p-4 for all 4*5 and 6*7 parameter pairs, and via the polygon endpoint v(b_1)=2p-3."
  },
  {
   "claim": "WII: ramification polygon of the Eisenstein polynomial Y^p - scY^{p-1} - saY^{p-3} - s is a single segment from (1,2p-3) to (p,p) of slope -(p-3)/(p-1)",
   "status": "proved",
   "notes": "Computed in truncated F_p[[y_0]] (my own series arithmetic) for every (a,c), a != 0, at p=5,7; interior points strictly above the segment in all cases. Hence all p-1 root differences have v_E(y_i - y_0) = 1 + (p-3)/(p-1)."
  },
  {
   "claim": "WII.2: I_infty = C_p semidirect C_{(p-1)/2} with single lower jump j = (p-3)/2, independent of q (all a != 0, all c)",
   "status": "proved",
   "notes": "Verified at p=5,7 by two independent routes on top of the polygon. (i) v_s(disc) = 2p-4 is even, so disc is a square in K^ur = F_p_bar((s)) (every unit there is a square), so the geometric group lies in A_p, excluding m = p-1 (whose generator is an odd (p-1)-cycle) from the dichotomy m in {(p-1)/2, p-1} forced by a(P) = d(E/K) = 2p-4 => j/m = (p-3)/(p-1). (ii) Independent tame-uniqueness argument: over K^ur the p-1 non-distinguished roots have valuation with exact denominator (p-1)/2, so F factors over E^ur into linear times two factors of degree (p-1)/2, each generating THE unique tame cyclic extension of that degree of E^ur; hence [L:K^ur] = p(p-1)/2, m = (p-1)/2. Single jump is automatic: the wild subgroup has prime order p. The doc's own gcd(j,m)=1 route (Serre theta^j on the jump quotient, faithful since I embeds in AGL(1,p)) is also sound. CONFIRMED."
  },
  {
   "claim": "Tame transpositions at t = +1 and t = -1, at critical points z = +1, z = -1 respectively, for every q != 0,2",
   "status": "proved",
   "notes": "In char p, f_q'(z) = 3(z^2-1)/(q-2) since (z^p)' = 0; verified by flint factorization for all q in F_p minus {0,2} at p=5,7: f_q - 1 has exactly one double root, located at z=1, all other factors simple (mult 1); f_q + 1 likewise at z=-1. e=2 tame since p>=5. Critical values +1, -1 distinct. CONFIRMED (and exactly matches the CRITICAL_VALUE_NORMAL_FORM claim)."
  },
  {
   "claim": "WII sections 4-5: tr(Lambda_p) = p on C_p - {1} and 0 elsewhere in I; dim Lambda_p^I = 2; dim Lambda_p^{C_p} = p-1; Swan_infty(Lambda_p) = -(p-3); a_infty(Lambda_p) = -(p-1)",
   "status": "proved",
   "notes": "Verified on the explicit permutation group C_p x| C_{(p-1)/2} inside S_p at p=5,7: exactly p-1 p-cycles (the nontrivial translations), all non-translations have >=2 cycles hence det(1-g|Std)=0; character averages give dim invariants 2 (over I) and p-1 (over C_p); Swan = (j/m)(0-(p-1)) = -(p-3); a = -2-2j = -(p-1). The stated identity Lambda_p|_I = (p/m)Ind_{C_p}^I(1) - (1/m)Reg_I checks as characters."
  },
  {
   "claim": "Consistency: d(L/K) = m(2p-4) + (m-1) = (pm-1) + j(p-1) (conductor transitivity vs filtration)",
   "status": "proved",
   "notes": "Both sides equal (p-1)(2p-3)/2 - 1; checked numerically at p=5 (13=13) and p=7 (32=32)."
  },
  {
   "claim": "Caveat: the ARITHMETIC Galois group of the local extension over F_p((s)) equals the inertia group",
   "status": "false",
   "notes": "For p=5, a=1, c=0: disc(F) = 3s^6 exactly (verified two ways), whose unit class 3 is a nonresidue mod 5, so the arithmetic group is NOT in A_5 while the geometric inertia D_5 is. The arithmetic group contains an extra unramified quadratic. WILD_INFINITY_INERTIA.md only claims the inertia group, so it is unaffected, but any future step that silently conflates arithmetic monodromy at infinity with I_infty (e.g. in Frobenius-constituent bookkeeping for the parity-pairing program) must track this unramified quadratic twist, whose class depends on (a,c)."
  },
  {
   "claim": "Branch script hook_cohomology_effectivity_ledger.py and its stored results JSON are correct for p=5,7,11,13",
   "status": "proved",
   "notes": "Ran the branch script live (--primes 5 7 11 13): PASS, all internal checks true; every per-hook row (rank, invariants, Swan, h1) and every total matches my independent brute-force code exactly; the committed hook_cohomology_effectivity_ledger_results.json totals also match."
  }
 ],
 "writeup": "TASK 1 - WALL B RE-DERIVATION (independent code, then branch comparison). My script /tmp/claude-0/-home-user-multi-agent-environment/53da20a7-5af0-58c9-b6a4-3bdefd3e2c90/scratchpad/my_ledger.py computes the ledger from scratch for p=5,7,11,13. Crucially, dim V_i^{C_p} is computed by BRUTE-FORCE enumeration of i-subsets of {1,...,p-1} with sum divisible by p (the representation-theoretic meaning of the invariants, since Std|_{C_p} is the sum of all nontrivial characters), not from the closed form; the closed form (C(p-1,i)+(p-1)(-1)^i)/p then agrees exactly in all 40 (p,i) cases, and is integral (HCE.1 CONFIRMED). Swan_i = ((p-3)/(p-1))(r_i - a_i) is an exact integer in all cases (HCE.2 integrality CONFIRMED); the derivation is the standard break-decomposition formula for a single break: Swan = sum_{i=1..j} |G_i|/|G_0| (dim - dim^{G_i}) = j * (p/(pm)) * codim = (j/m)*codim with j=(p-3)/2, m=(p-1)/2 - each of the r_i - a_i wild characters carries break (p-3)/(p-1) < 1. h^1_c = r_i + s_i + [i=0] follows from GOS with chi_c(U) = 2 - 0 - 3 = -1 on the 3-punctured line (only the wild point contributes Swan; t=+-1 are tame), h^0_c = 0 (affine curve), h^2_c = coinvariants = [i=0] because the global geometric monodromy is all of S_p (transitive, contains a transposition from t=+-1 and a p-cycle from I_infty; a transitive group with a p-cycle and a transposition on p letters is S_p) and V_1..V_{p-1} are nontrivial irreducibles. Totals verified for p=5,7,11,13: total h1 = 23, 101, 1769, 7247 = ((2p-3)2^{p-1}+3)/p exactly (with p | (2p-3)2^{p-1}+3 checked); even/odd = 11/12, 49/52, 881/888, 3619/3628; alternating = -1,-3,-7,-9 = 4-p; alternating Swan = -(p-3); even = (T+4-p)/2 and odd = (T-4+p)/2. I then ran the branch script (frontier/d1_discriminant/hook_cohomology_effectivity_ledger.py) live with --primes 5 7 11 13: status PASS, and EVERY per-hook row (i, rank, cp_invariants, swan_infinity, h_c_1) and every total matches my independent output exactly; the committed results JSON (frontier/d1_discriminant/hook_cohomology_effectivity_ledger_results.json) also matches. Wall B: CONFIRMED (conditional only on the wild-inertia input, verified below).\n\nTASK 2 - WILD_INFINITY_INERTIA.md (frontier/d1_discriminant/WILD_INFINITY_INERTIA.md), verified by direct local computation at p=5,7 in /tmp/claude-0/-home-user-multi-agent-environment/53da20a7-5af0-58c9-b6a4-3bdefd3e2c90/scratchpad/wild_check.py, sweeping ALL (a,c) with a != 0 (30 pairs at p=5, 42 at p=7) and all valid q.\n\n(A) WII.1 different exponent: with y=1/x, s=1/t, the local equation is s = y^p/(1+a y^{p-3}+c y^{p-1}); symbolic char-p differentiation gives numerator lowest term exactly 3a*y^{2p-4} (denominator a unit), so v_y(ds/dy) = 2p-4 iff a != 0, for ANY c (q-independence at infinity is unconditional, not just generic). CONFIRMED.\n\n(B) Ramification polygon: F(Y) = Y^p - scY^{p-1} - saY^{p-3} - s is Eisenstein over K = F_p((s)); working in E = F_p((y_0)) with s expanded as a power series in the root y_0, the Newton polygon of F(y_0(1+x)) is a single segment from (1, 2p-3) to (p, p), slope -(p-3)/(p-1), interior points strictly above, for every (a,c), a != 0, at p=5,7. So all p-1 root differences have v_E(y_i-y_0) = 1 + (p-3)/(p-1). CONFIRMED.\n\n(C) Group and jump: the wild subgroup of I has order exactly p (e = p * tame), and a group of prime order has automatically a SINGLE lower jump - so 'one jump' needs no computation; the content is (j,m). a(Perm) = d(E/K) = 2p-4 = (p-1) + j(p-1)/m forces j/m = (p-3)/(p-1), whose reduced form has denominator (p-1)/2, so m in {(p-1)/2, p-1}. Two independent verifications pick (p-1)/2: (i) disc(F) computed exactly (resultant with the char-p derivative, F monic so reduction commutes) has v_s(disc) = 2p-4, EVEN, for all (a,c); over K^ur = F_p_bar((s)) every unit is a square, so disc is a square there, so the geometric Galois group (= inertia) lies in A_p; the m = p-1 group AGL(1,p) contains an odd (p-1)-cycle, hence is excluded; the m=(p-1)/2 group is even. (ii) Independent of disc: over E^ur the p-1 other roots have valuation 1+(p-3)/(p-1) with exact denominator (p-1)/2, so each irreducible factor of F/(Y-y_0) over E^ur has degree >= (p-1)/2; since they sum to p-1 there are exactly two factors of degree (p-1)/2, each totally tamely ramified, and over an algebraically closed residue field the tame cyclic extension of a given degree is UNIQUE, so both generate the same extension M and [L:K^ur] = p(p-1)/2, i.e. m = (p-1)/2 and j = m(p-3)/(p-1) = (p-3)/2. The doc's own route (Serre's theta^j action on the jump quotient plus faithfulness, which holds because a transitive solvable subgroup of S_p sits in AGL(1,p) where point stabilizers embed in F_p^*) is also sound. Transitivity cross-check d(L/K) = m(2p-4)+(m-1) = (pm-1)+j(p-1) verified (13=13 at p=5, 32=32 at p=7). WII.2 CONFIRMED.\n\nIMPORTANT CAVEAT DISCOVERED: over the actual local field F_p((s)) the disc unit class can be a NONRESIDUE (p=5, a=1, c=0: disc = 3s^6 exactly, checked by two methods), i.e. the ARITHMETIC Galois group is strictly larger than the inertia by an unramified quadratic. A naive disc-square test over F_p((s)) would falsely refute WII.2; the doc's claim is about inertia and is correct. Any future Frobenius-level bookkeeping at infinity (exactly what the surviving parity-pairing door needs) must carry this (a,c)-dependent unramified quadratic twist.\n\n(D) Tame points: in char p, f_q'(z) = 3(z^2-1)/(q-2) (the z^p term differentiates to 0), so the finite critical points are z = +-1, simple, with critical values f_q(1) = 1, f_q(-1) = -1 exactly. Flint factorization for every q in F_p minus {0,2} at p=5,7 confirms: f_q - 1 has exactly one repeated factor, (z-1)^2, all else simple; f_q + 1 has exactly (z+1)^2. Hence local monodromy at t=+-1 is a single transposition, tame (e=2, p>=5), critical values distinct. CONFIRMED, and consistent with CRITICAL_VALUE_NORMAL_FORM.md.\n\n(E) WII sections 4-5 on the explicit group I = C_p x| C_{(p-1)/2} built as permutations of F_p at p=5,7: exactly p-1 elements are p-cycles (nontrivial translations); every non-translation fixes a point and has >= 2 cycles, so tr(g|Lambda_p) = det(1 - g|Std) = 0 for them and = p on the p-cycles; character averaging gives dim Lambda_p^I = 2 and dim Lambda_p^{C_p} = p-1; Swan(Lambda_p) = (j/m)(0-(p-1)) = -(p-3) and a_infty = -(p-1). The stated identity Lambda_p|_I = (p/m)Ind_{C_p}^I(1) - (1/m)Reg_I checks as characters. CONFIRMED.\n\nVERDICTS: WII.1 CONFIRMED; single lower jump (p-3)/2 CONFIRMED; I_infty = C_p x| C_{(p-1)/2} CONFIRMED; tame transpositions at t=+-1 CONFIRMED; q-independence CONFIRMED and in fact uniform in (a != 0, any c; any q != 0,2); Lambda_p conductor claims CONFIRMED; hook ledger (HCE.1-4) CONFIRMED and branch script + stored JSON reproduce my independent computation exactly. No claim of either document was refuted; one latent hazard (arithmetic vs geometric monodromy at infinity) documented.\n\nFiles: my code at /tmp/claude-0/-home-user-multi-agent-environment/53da20a7-5af0-58c9-b6a4-3bdefd3e2c90/scratchpad/my_ledger.py and .../wild_check.py; branch files read via git show origin/gpt56/consolidated-fortune-20260721: frontier/d1_discriminant/WILD_INFINITY_INERTIA.md, frontier/d1_discriminant/HOOK_COHOMOLOGY_EFFECTIVITY_LEDGER.md, frontier/d1_discriminant/hook_cohomology_effectivity_ledger.py, frontier/d1_discriminant/hook_cohomology_effectivity_ledger_results.json."
}