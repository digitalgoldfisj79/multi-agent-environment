# Scope correction: the canonical configuration face complex is a narrow no-go, not a closure of Route B

**Date:** 2026-07-25  
**Branch:** `gpt56/d1-main-twisted-descent-20260724`  
**Status:** **AUTHORITATIVE CORRECTION** to `RELATIVE_CONFIGURATION_COMPLEX_OBSTRUCTION_20260725.md` and `MAIN_BRANCH_STATUS_AFTER_CONFIGURATION_OBSTRUCTION_20260725.md`.

## Correction

The earlier status promoted the exactness of the canonical relative deletion complex to a theorem-level obstruction closing the configuration-space route. That promotion was too strong.

The intact programme already distinguishes three possible locations for the required differential:

1. after parabolic pushforward over the `t`-line;
2. globally over the `(q,t)`-surface;
3. inside the Fourier--Cayley presentation of the sparse ordered-root section.

The proved calculation addresses only the canonical pre-pushforward deletion/insertion complex of generic lisse `S_p`-sheaves.

## What remains proved

Let `P=Perm_p=1+Std`. The alternating deletion maps form the augmentation Koszul complex

\[
0\to\bigwedge^pP\to\cdots\to\bigwedge^1P\to\mathbf1\to0.
\]

It is exact and its Euler class is

\[
\lambda_{-1}(P)=\lambda_{-1}(1)\lambda_{-1}(Std)=0.
\]

It is therefore not the irreducibility-hook object

\[
\lambda_{-1}(Std)=\sum_i(-1)^i\bigwedge^iStd.
\]

The candidate fails all three required regressions and must be rejected:

- it does not produce the weight-zero Kummer line;
- it does not produce the complete `p=5` Kummer plus pair minus `D_q` decomposition;
- it does not produce the nonzero `p=7` `V_3-V_4` block.

The exact virtual-rank identity also remains valid:

\[
\operatorname{vrank}\mathcal M_q
=p-3-2\left\lfloor\frac{p-1}{4}\right\rfloor.
\]

## What is retracted

The following conclusions are retracted:

1. that the generic `K_0(S_p)` lower bound closes any complex formed after parabolic pushforward;
2. that Schur's lemma forbids differentials between the resulting hook multiplicity or cohomology complexes;
3. that the Phase-3 actual semisimplified rank gate has been decided;
4. that stopping condition 4 of the programme has been reached.

The generic `K_0(S_p)` argument proves only that no generically exact complex of semisimple `S_p`-local systems can itself have hook Euler class. It does not control a differential constructed after applying `Rj_*`, parabolic cohomology, nearby cycles, Fourier transform, or global `(q,t)` pushforward.

Similarly, `Hom_{S_p}(V_i,V_j)=0` for `i!=j` rules out a direct generic map of irreducible local systems. It does not rule out a boundary or Fourier differential between their multiplicity complexes after equivariant pushforward.

## Correct branch ruling

The function-field crown remains open. The actual rank of the semisimplified even/odd middle survivor remains unknown. The following routes remain live:

- the post-parabolic configuration complex;
- the global `(q,t)` collision complex;
- the global `S_p`-equivariant Fourier--Cayley decomposition;
- direct invariant/quadratic q-line trace control;
- a non-face Frobenius/Adams correspondence.

The old verifier remains valid as a regression test for the rejected canonical face complex. It is not a verifier of route closure.
