# Main-branch status after cyclic regularity and the odd-extension bridge

**Date:** 2026-07-24  
**Branch:** `gpt56/d1-main-twisted-descent-20260724`  
**Scope:** function-field `d=1` Fortune sibling only.

## New exact position

The smooth primitive motive

\[
H_p=H^{p-5}_{prim}(X_p^{perm})
\]

has exponential rank

\[
\frac{2^{p-1}-1}{3},
\]

but its restriction to the cyclic subgroup generated by the `p`-cycle is exactly regular:

\[
H_p|_{C_p}
\cong
\mathbf Q_\ell[C_p]^{\oplus q_p},
\qquad
q_p=\frac{2^{p-1}-1}{3p}.
\]

The affine normalizer decomposition gives two Frobenius multiplicity spaces of equal rank:

\[
H_p
\cong
M_{0,p}\oplus(\rho_p\otimes M_{1,p}),
\qquad
\mathcal D_p=M_{0,p}-M_{1,p}.
\]

The target sum is

\[
T_p=p^2\operatorname{Tr}(F|\mathcal D_p).
\]

Thus the analytic problem is now a rank-zero Frobenius correlation between two equal-rank multiplicity spaces. It is not a generic estimate on the full primitive cohomology.

## Exact Airy comparison

Let

\[
\mathcal R_p=U_p-U_{p-2}(-1)
\]

be the common-weight Airy virtual module. For every extension degree

\[
\gcd(r,2p)=1,
\]

one has

\[
\operatorname{Tr}(F^r|\mathcal R_p)
=
\operatorname{Tr}(F^r|\mathcal D_p(-3)).
\]

Therefore

\[
\mathcal E_p=\mathcal R_p-\mathcal D_p(-3)
\]

has zero Frobenius trace in every degree coprime to `2p`.

This is an exact infinite-family trace comparison, not a first-trace coincidence.

## What remains on the analytic branch

The first trace is still

\[
|\operatorname{Tr}(F|\mathcal D_p)|
\le C p^{(p-5)/2}.
\]

The new representation theorem identifies the two spaces whose Frobenius traces must correlate. A proof must now construct one of:

1. a Frobenius-equivariant map between `M_{0,p}` and `M_{1,p}` with bounded trace cone;
2. an exact formula for `D_p` as a small virtual Weil module;
3. a uniform cancellation theorem for the difference of the two multiplicity traces.

Generic Weil bounds, bare cyclic fixed loci, coefficient resonance and more prime sweeps remain non-decisive.

## What remains on the application branch

The discrepancy

\[
\mathcal E_p=\mathcal R_p-\mathcal D_p(-3)
\]

is invisible in all degrees coprime to `2p`. It can only be detected by the two omitted sectors:

1. **even degrees**, where the `mu_3` projector requires twisted Lefschetz terms and the arithmetic quadratic twist appears;
2. **degrees divisible by `p`**, where the Artin--Schreier descent algebra splits and the cyclic twist untwists.

The next application theorem must compute these two sectors and match them with the existing boundary ledger:

- `q=2`;
- `q=infinity`;
- arithmetic quadratic twist;
- Artin--Schreier/main/Tate subtraction;
- endpoint/punctual contribution;
- parity-protected irreducibility certificate.

This is now the complete admissible application task. A broad search for a direct injection from the exponential primitive motive into the `O(p)` hook survivor is closed.

## Stop rule

Continue only with:

- a twisted `mu_3` Lefschetz calculation for the even sector;
- an exact split-algebra comparison for the `p`-divisible sector;
- or a Frobenius map between `M_0` and `M_1`.

Anything else is outside the two main branches.
