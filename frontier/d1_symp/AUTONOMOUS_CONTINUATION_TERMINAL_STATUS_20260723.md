# Autonomous continuation — terminal audited status

**Date:** 2026-07-23  
**Branch:** `gpt56/d1-airy-final-audit-20260723`  
**Scope:** function-field `d=1` Fortune sibling only; integer Fortune untouched.

## Results obtained

1. Corrected the global `T_p` sign against the explicit hyperplane definition.
2. Proved the odd-power `mu_3` trace-extraction lemma.
3. Computed exact `F^3` traces on the invariant Airy spaces at `p=11,17,23,29` by modular DFT and CRT, with independent field-model/residue validation.
4. Reconstructed complete exact invariant spectra in ranks `1--4`, except for the non-load-bearing determinant sign of trace-zero `U_15`.
5. Found one exact central common factor at `p=23`.
6. Refuted uniform common-factor, root-of-unity-phase and slope-by-slope-pairing models.
7. Recomputed the `10^5` sweep and corrected its extreme-value calibration: the observed maximum is low under the iid Gaussian null and does not support failure of an absolute constant.
8. Audited the parent dependency: absolute `C` is required by the stated Airy half-theorem, but the categorical implication from that theorem to the Fortune crown has not been proved.
9. Enumerated the exact missing application-ledger obligations and re-audited every remaining route.

## Current mathematical status

### Proved

- collapse identity and integrality;
- nonzero-fibre constancy for `p ≡ 2 (mod 3)`;
- odd-power trace extraction;
- self-dual spectral constraints;
- all prior no-go/failure certificates not superseded by the sign correction.

### Exact computer-assisted theorems

- separated first traces and the new third traces;
- low-rank characteristic polynomials and gcds;
- corrected sweep statistics and exact iid-normal maximum calibration.

### Open theorem A

\[
|\operatorname{Tr}(F|U_p)-p\operatorname{Tr}(F|U_{p-2})|
\le C p^{(p+1)/2}
\]

with an absolute `C`.

### Open theorem B

Construct and verify the endpoint/main/Tate/Artin--Schreier/nearby-cycle ledger transporting the Airy deviation bound into the exact `FF-Fortune(p,1)` irreducibility certificate.

## Stop ruling

The autonomous programme has reached a genuine research boundary. Additional finite-prime computation cannot prove either open theorem, and all located standard mechanisms have been either applied or supplied with an exact failure certificate. Progress now requires a new wild characteristic-`p` global correlation theorem, a new categorical application identity, or external specialist input producing one of those.
