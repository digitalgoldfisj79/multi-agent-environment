# Authoritative d=1 status after cyclic Fourier and Tate diagonal

**Date:** 2026-07-25  
**Branch:** `gpt56/d1-main-twisted-descent-20260724`  
**Scope:** function-field `d=1` sibling only; integer Fortune and publication editing are excluded.  
**Status:** supersedes the terminal-frontier section of `MAIN_BRANCH_STATUS_AFTER_FULL_WEIL_BRIDGE_20260724.md`.

## 1. Position

The crown remains open. The two admitted branches have nevertheless advanced beyond the previous generic obstruction statements.

### Application branch

The nonzero-frequency alternating-hook object is now explicit. The complete virtual hook is one cyclic induction difference, its affine Fourier trace is one generalized Airy family over `F_(q^p)`, and every nonzero phase has no finite critical point on the degree-`p` root locus.

Therefore the open application theorem is not a generic stationary-phase problem. It is a wild-infinity nearby-cycle theorem at the frequency-origin degree drop.

### Analytic branch

The exceptional identity `k=p=char` has an exact modular mechanism. Cyclic Tate cohomology of the `p`-fold tensor power is the Frobenius twist, and the rank-two Adams difference contracts to that twist in characteristic `p`.

Therefore the open analytic theorem is not missing local geometry. It is an integral lifting theorem from modular Smith/Tate contraction to a characteristic-zero Weil trace, or absolute cancellation inside the linearly growing cone of the natural lift.

## 2. PROVED: cyclic form of the complete hook projector

Let `C=<sigma>` be generated by a `p`-cycle and let `xi` be any nontrivial character of `C`. Then

\[
\boxed{
\sum_{i=0}^{p-1}(-1)^i\bigwedge^i\mathrm{Std}
=
\operatorname{Ind}_C^{S_p}{\bf1}
-
\operatorname{Ind}_C^{S_p}\xi.
}
\]

Hence for every `S_p`-equivariant Weil complex `K`,

\[
\boxed{\operatorname{HookAlt}(K)=K^C-K_\xi.}
\]

This recovers `K_ambient=D_p` and applies unchanged to the full Fourier localization triangle. A hook-by-hook Fourier decomposition is unnecessary.

## 3. PROVED: exact affine nonzero-frequency trace

For `q=p^r`, `L=F_(q^p)` and

\[
P_\lambda(T)=\sum_{m=4}^{p-4}\lambda_mT^m,
\]

the affine monic hook Fourier trace is

\[
\boxed{
h_q(\lambda)=
\sum_{\substack{\alpha\in L\\
Tr(\alpha)=Tr(\alpha^2)=Tr(\alpha^3)=0}}
\psi_q(Tr(P_\lambda(\alpha)))-q.
}
\]

Equivalently,

\[
\boxed{
h_q(\lambda)=q^{-3}\sum_{u_1,u_2,u_3\in F_q}
\sum_{\alpha\in L}
\psi_q(Tr(P_\lambda(\alpha)+u_1\alpha+u_2\alpha^2+u_3\alpha^3))-q.
}
\]

This is a generalized Airy family. At `lambda=0` its degree drops to three.

## 4. PROVED: finite stationary phase is absent

For nonzero `(lambda,u)`, the derivative of the phase is nonzero of degree at most `p-5`. A root in `F_(q^p)` has degree over `F_q` equal to one or `p`; it cannot have degree `p` and satisfy that derivative.

Thus there are no finite critical points on the irreducible degree-`p` locus. Any load-bearing open-frequency contribution must arise from wild infinity, the discriminant, or the explicit quotient/boundary ledger.

Combined with the proved absence of the half-twisted Airy constituent from canonical zero frequency, the only possible Airy transport is through nearby cycles at infinity as the generalized Airy degree drops at the frequency origin.

## 5. PROVED: modular Tate diagonal

For a perfect field `k` of characteristic `p` and a finite-dimensional `V`, cyclic permutation gives

\[
\boxed{
\widehat H^n(C_p,V^{\otimes p})\cong V^{(1)}
}
\]

in every Tate degree. The isomorphism is induced by `v -> v^(tensor p)`.

For rank two,

\[
0\to E^{(1)}\to Sym^pE\to det(E)\otimes Sym^{p-2}E\to0,
\]

so the modular Adams difference is exactly the Frobenius contraction. Pointwise,

\[
Tr(\sigma A^{\otimes p})=Tr(A^p)=\alpha^p+\beta^p.
\]

This identifies the canonical characteristic-`p` mechanism behind the Airy pair `R_p`.

## 6. Exact application theorem

Construct a weighted projective compactification of

\[
P_\lambda(T)+u_1T+u_2T^2+u_3T^3
\]

and compute the `C`-trivial minus `C`-nontrivial nearby-cycle complex at infinity over `lambda=0`. Prove that its weight-two semisimple part is

\[
R_p((p-1)/2)
\]

and identify its complement with `S_0`, `S_chi`, `q=2`, `q=infinity`, discriminant, punctual, Tate and Artin--Schreier terms.

This is a characteristic-`p` divided-power/wild-infinity analogue of the tame secant-support theorem.

## 7. Exact analytic theorem

Construct a Frobenius-compatible integral Smith/Tate comparison for

\[
R\Gamma_c(A^1,\mathscr A_O^{\otimes p})^{\mu_3}
\]

whose generic cyclic trace is `R_p` and whose modular localization is the Frobenius contraction, such that after removing Chuang's single Tate line the generic comparison cone either:

1. has uniformly bounded total rank; or
2. has Frobenius trace bounded by `C p^((p+1)/2)` with absolute `C`.

A modular comparison alone is insufficient: the Tate quotient forgets the characteristic-zero eigenvalues of free cyclic orbits. The natural Dwork lift has full surviving rank `(p-5)/6` and is not a bounded-cone lift.

## 8. Closed routes

Do not return to:

- finite interior stationary phase on the irreducible root locus;
- canonical zero frequency;
- local vanishing cycles of the transverse sparse section;
- a hook-by-hook transform;
- the unmodified bounded-cone Dwork lift;
- raw prime sweeps or generic Weil bounds.

## 9. Current ruling

### PROVED

- all earlier Airy--cyclic and q-line reductions;
- cyclic induction form of the complete hook;
- exact generalized-Airy Fourier trace;
- absence of finite critical points at nonzero frequency;
- modular cyclic Tate/Frobenius contraction.

### OPEN

- the wild-infinity nearby-cycle theorem;
- an integral Weil-compatible Tate lift or equivalent global correlation theorem;
- the crown.

Further progress must act directly on one of those two named objects.
