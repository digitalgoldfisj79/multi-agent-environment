# Programme status after the sign trace and exact `p=13` bar obstruction

**Date:** 2026-07-26  
**Branch:** `gpt56/fortune-strategy-synthesis-audit-20260726`  
**Target:** function-field Fortune crown at `d=1`.  
**Status:** two exact phases completed; the raw terminal-bar route has reached a theorem-level obstruction. The crown remains **OPEN**.

## 1. Exact aggregate target

For the full four-parameter interval and Sawin's aggregate hook representations,

\[
B_\Lambda=B(\pi_+)+B(\pi_-),
\]

the crown follows from

\[
\boxed{B_\Lambda\le p-1.}
\]

This remains the authoritative full-interval target.

## 2. Completed theorem I: exact sign-hook extraction

The full sign factorization-function trace is

\[
\boxed{
S_{\mathrm{sgn}}(p)
=
\frac{1-\chi(-1)}2\chi(-6)p^2(p-1).
}
\]

For admitted primes `p congruent 5 mod 6`,

\[
S_{\mathrm{sgn}}(p)=
\begin{cases}
0,&p\equiv5,17\pmod{24},\\
+p^2(p-1),&p\equiv11\pmod{24},\\
-p^2(p-1),&p\equiv23\pmod{24}.
\end{cases}
\]

After removing the trivial and sign hooks, write the remaining aggregate constant as `B_mid`. If

\[
B_{\mathrm{mid}}\le p-1,
\]

then the Sawin lower bound closes the crown immediately in the residue classes

\[
p\equiv5,11,17\pmod{24}.
\]

In the sole marginal class

\[
p\equiv23\pmod{24},
\]

the crude triangle bound lands exactly at the weighted threshold `p^2`. The remaining task there is only to exclude complete saturation:

\[
E_{\mathrm{mid}}=-(p-1)p^3.
\]

A lower-weight class, a non-aligned Frobenius phase, a one-unit Betti improvement or a congruence excluding equality would suffice.

## 3. Completed theorem II: exact `p=11` terminal profile

The characteristic-zero hook-nullity profile of the order-11 shuffle operator is

\[
(0,0,1,1,1,3,3,1,0,0,1).
\]

The multiplicity-one total is `11`; the unique sign hook contributes one; deleting it leaves

\[
10=p-1.
\]

Thus at `p=11`, sign extraction exactly reconciles the terminal multiplicity-one bar mass with the aggregate budget.

## 4. Completed theorem III: this pattern fails at `p=13`

At `p=13`, exact cyclotomic certificates prove

\[
\dim\ker\Omega|_{\wedge^3}=2,
\quad
\dim\ker\Omega|_{\wedge^4}=5,
\quad
\dim\ker\Omega|_{\wedge^5}=5,
\quad
\dim\ker\Omega|_{\wedge^6}=5.
\]

These four non-sign hooks alone contribute

\[
17>12=p-1.
\]

Hence the unmodified `C_wedge` terminal first-homology page is not uniformly within the Sawin budget, even after sign deletion and before any other hooks are counted.

This proves that the `p=11` sign-only explanation is exceptional as a complete terminal-budget mechanism.

## 5. Exact scientific ruling

The following route is closed:

> full configuration-space `C_wedge` terminal bar homology  
> `+` delete the sign hook  
> `+` divide by multiplicity two  
> `=> B_Lambda<=p-1`.

It fails already at `p=13`.

The failure does not refute the admitted crown because `13 congruent 1 mod 6`. It proves that a valid comparison must use geometry absent from the raw full-configuration-space bar object.

At least one of the following must occur after restriction to the sparse four-parameter interval:

1. new differentials in the Fourier--Cayley/Rees spectral sequence;
2. a canonical quotient imposed by the sparse coefficient equations;
3. weight exclusion preventing terminal bar classes from entering non-top compactly supported cohomology;
4. arithmetic projectors killing whole hook sectors.

At `p=13`, such a mechanism must remove at least five multiplicity-one non-sign dimensions from hook degrees `3` through `6` alone.

## 6. First admitted diagnostic

For `p=17`, modular computations at two independent auxiliary primes give

\[
\dim\ker(\wedge^3)=2,
\qquad
\dim\ker(\wedge^4)=8.
\]

These values are stable but not yet characteristic-zero theorems. Their total `10` remains below the admitted budget `16`, so they do not decide the admitted case. Computing larger raw hook matrices without first constructing the sparse differential has sharply declining value.

## 7. Revised programme in dependency order

### Phase 0 — completed

- audit Sawin's aggregate packaging;
- establish the exact target `B_Lambda<=p-1`;
- separate unsigned Betti mass from virtual traces.

### Phase 1 — completed

- compute the exact `p=11` terminal profile;
- identify the sign-hook overage;
- evaluate the sign trace exactly over the full interval.

### Phase 2 — completed and stopped

- test whether sign deletion controls the raw terminal page;
- exact `p=13` calculation disproves the uniform sign-only mechanism.

### Phase 3 — active

Construct the **sparse-section terminal cancellation theorem**:

> Build a parity-separated, Frobenius-compatible Rees filtration from the wild nonzero-frequency Fourier--Cayley complex of the four-parameter sparse interval to the full configuration-space quantum-bar object. Determine the additional differentials, quotients, weight truncations and arithmetic projectors introduced by the sparse section. Prove that, after exact sign extraction, the surviving multiplicity-one non-top associated-graded mass is at most `p-1` for primes `p congruent 5 mod 6`.

The theorem must explain the exact `p=13` regression by killing at least five known non-sign bar classes; this is a concrete falsification test for any proposed construction.

### Phase 4 — marginal residue class

For `p congruent 23 mod 24`, supplement the mass bound with a nonsaturation theorem excluding

\[
E_{\mathrm{mid}}=-(p-1)p^3.
\]

### Phase 5 — conclusion

Insert the exact sign trace and the sparse associated-graded bound into Sawin's weighted interval inequality, prove the weighted sum exceeds `p^2`, and conclude the `d=1` crown.

## 8. Stop rule

Do not spend the main programme on:

- larger unmodified `C_wedge` bar profiles;
- scalar virtual Airy transport presented as a Betti estimate;
- fixed-class `h=2` absolute Weil bounds;
- empirical density recentering without a proved constituent;
- integer Fortune.

The next advance must construct or refute the sparse-section differential/quotient. The raw terminal algebra has now supplied all the decision value it can.
