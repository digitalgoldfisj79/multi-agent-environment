# Fresh-model review prompt

Review the current integer Fortune programme as a hostile independent analytic number theorist.

## Repository state

Repository:

`digitalgoldfisj79/multi-agent-environment`

Branch:

`gpt56/fortune-mesoscopic-cotlar-20260728`

Cold-review packet:

`frontier/integer_source_frame_bridge/FRESH_MODEL_REVIEW_PACKET_PUNCTURED_CENTRE_20260729.md`

Mathematical review base head:

`224d670d98cc4bf46c401569758c91537b78b46a`

The later commits only add the review packet and prompt.

## Mandatory reading order

1. `FRESH_MODEL_REVIEW_PACKET_PUNCTURED_CENTRE_20260729.md`
2. `PROGRAMME_STATUS_AFTER_PUNCTURED_CENTRE_GATE_20260729.md`
3. `PRIME_SOURCE_TWO_LEVEL_IDENTITY_AND_PUNCTURED_CENTRE_20260729.md`
4. `PUNCTURED_CENTRE_FULL_BAND_GATE_20260729.md`
5. `prime_source_heath_brown_verify.py`
6. `punctured_centre_offdiagonal_verify.py`
7. the corresponding frozen JSON result files

Use the older branch notes only when needed to verify definitions or dependencies.

## Review rules

- Re-derive all load-bearing identities independently.
- Do not infer asymptotic truth from finite verifier success.
- Do not accept a theorem citation unless you map every parameter and hypothesis literally.
- Keep separate: proved algebra, classical estimates, machine verification, empirical evidence and open claims.
- Search aggressively for endpoint errors, omitted support classes, incorrect centring, lost diagonal terms, invalid source-cell recombination and hidden uses of triangle inequality.
- Treat the assertion that the source-product diagonal is Fortune-admissible as a claim to audit, not a premise.
- Treat the claim that only the genuinely two-variable determinant stratum survives as a claim to audit, especially at dyadic endpoints.

## Core claims to assess

The branch claims that for `H=eta X^2<X^2` and `Y=ceil(sqrt(H))<X`:

1. the exact two-level Heath--Brown identity resums to
   
   `Lambda = mu_<=Y * log + mu_<=Y * mu_>Y * 1 * log`
   
   on `n<=H`;
2. there is therefore only one explicitly small Möbius variable `d`;
3. every nonzero such `d` divides every primorial centre `P_j`;
4. the congruence transports exactly to the punctured centre `P_j/d`;
5. after complete Euler-band recombination, the exact source-product diagonal is below the Fortune block allowance by a polynomial factor;
6. after removing product equality, the one-variable collision strata disappear and the remaining low-mode collision condition is
   
   `p | d m - d' m'`, with `d != d'` and `m != m'`;
7. the fixed-modulus residue energy is the Möbius/Heath--Brown weighted multiplicative character fourth moment displayed in the packet;
8. no currently identified black-box theorem closes the required coherent cross-modulus and cross-conductor estimate.

## Required output

Produce a single review report with these sections.

### 1. Executive verdict

State whether the programme has:

- a valid new reduction;
- a repairable technical flaw;
- a fatal flaw;
- or an already-known theorem that closes the remaining gap.

### 2. Claim-by-claim audit

For each core claim, give one of:

- VALID;
- VALID WITH CONDITIONS;
- INVALID;
- NOT ESTABLISHED.

Show the derivation or counterexample.

### 3. Scale ledger

Recompute all powers of `X` and `log X`, including:

- source length;
- small-variable length;
- centre-block length;
- first-band modulus count;
- source diagonal;
- best justified off-diagonal estimate;
- Fortune allowance;
- exact remaining loss.

### 4. Literature audit

Search for the closest theorem on:

- Möbius-weighted multiplicative energy;
- fourth moments of short character sums with one Möbius factor;
- bilinear forms in multiplicative characters averaged over prime moduli;
- dispersion with varying primorial shifts;
- all-order sieve-survivor or conductor recombination.

For each candidate theorem, provide a literal parameter map and explain precisely why it applies or fails.

### 5. Best path forward

Give one sequenced programme aimed at the actual remaining theorem. Prioritise exact structural cancellation before generic inequalities. Identify the first statement that would constitute genuinely new mathematics.

### 6. Corrected boundary

End with a strict table:

- PROVED;
- VERIFIED ONLY;
- HEURISTIC/EMPIRICAL;
- OPEN;
- RETRACTED OR NEEDS CORRECTION.

Do not merely summarise the branch. Try to break it.
