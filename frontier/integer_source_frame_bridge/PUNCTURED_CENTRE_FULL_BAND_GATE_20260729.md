# Punctured-centre full-band gate and the weighted determinant obstruction

Date: 29 July 2026  
Branch: `gpt56/fortune-mesoscopic-cotlar-20260728`

Status: Gates A and B of the full-band programme pass.  The source identity has been reduced to one small Mobius variable, the complete source-product diagonal is Fortune-admissible, and the one-variable collision strata collapse to the removed source diagonal in every critical balanced cell below the first-band moduli.  The remaining low-mode kernel is the genuinely two-variable weighted congruence `p | dm-d'm'`, coherently summed across physical moduli and the higher survivor orders.  Existing multiplicative-energy and Kloosterman technology does not close that coherent full-band theorem at the required scale.  Fortune's conjecture remains **OPEN**.

## 1. Exact reduced source

With

\[
Y=\lceil\sqrt H\rceil<X,
\]

the exact identity is

\[
\Lambda
=
\mu_{\le Y}*\log
+
\mu_{\le Y}*\mu_{>Y}*1*\log
\qquad(n\le H).
\tag{1.1}
\]

Every nonzero `mu(d)` with `d<=Y` satisfies `d|P_j`, so

\[
p\mid P_j+dm
\iff
p\mid P_j/d+m.
\tag{1.2}
\]

Thus every dyadic source cell has the punctured-centre form

\[
\mathcal A_{j,R}
=
\sum_{d\sim D\atop d\le Y}\mu(d)
\sum_{m\sim M}c_d(m)
\,g_R\!\left(-m(P_j/d)^{-1}\right),
\tag{1.3}
\]

with the exact coefficient `c_d` inherited from (1.1).

## 2. Gate B: the source-product diagonal passes

After all signed source cells are recombined, the source is the prime indicator plus the explicit prime-power correction.  For one centre, put

\[
\delta_R=V_R^{-1}-1.
\]

The complete survivor coordinate has value `delta_R` on a surviving source and `-1` on a hit source.  If `M_Z` is the number of prime sources and `H_{j,R}` the number hit by the band, then the exact source diagonal is

\[
\boxed{
\mathcal D_{j,R}
=
(M_Z-H_{j,R})\delta_R^2+H_{j,R}.
}
\tag{2.1}
\]

Since `delta_R=O(1/log X)`, trivially

\[
\mathcal D_{j,R}\ll M_Z\ll H/\log H.
\tag{2.2}
\]

The frozen coefficient satisfies `beta_j << log X` on the established branch.  Hence

\[
\sum_{j\in B}\beta_j^2\mathcal D_{j,R}
\ll
K H\log X,
\tag{2.3}
\]

which is smaller than the Fortune block allowance

\[
K H X/\log X
\tag{2.4}
\]

by `X/log^2 X`.  The prime-power correction is smaller still because it has support `O(sqrt H)`.

Therefore the full-band route does not fail on the product diagonal.

## 3. Exact punctured-centre Gram

For a first-band prime `p`, define

\[
\Phi_{j,d}(p,a)
=
\frac1p e\!\left(\frac{a(P_j/d)}p\right).
\tag{3.1}
\]

The fixed-modulus Gram is

\[
\boxed{
G_p((j,d),(k,d'))
=
\frac1p\mathbf1_{p\mid P_jd'-P_kd}-\frac1{p^2}.
}
\tag{3.2}
\]

For fixed `j`, the map `d -> P_j/d mod p` is injective because `d<p`.  The residue multiplicity over a logarithmic centre block is therefore at most `K`, recovering the bounded punctured-centre synthesis frame for `K << log X`.

This exact Gram does not by itself remove the small-variable norm.  Its diagonal contains

\[
\frac1p\sum_{d\le Y}\mu(d)^2
\asymp
\frac Yp,
\tag{3.3}
\]

and summing the physical primes reproduces the `X/log X` loss unless the remaining source variable is used jointly.

## 4. Source diagonal removal and collision strata

For a balanced product cell `n=dm` with `d,m<p`, the low-mode collision condition between two source tuples is

\[
\boxed{p\mid dm-d'm'.}
\tag{4.1}
\]

The integer source-product diagonal `dm=d'm'` has already been removed.

If `d=d'`, then

\[
p\mid d(m-m')
\iff
m=m',
\]

because `|m-m'|<p`.  Likewise, if `m=m'`, then collision forces `d=d'`.  Therefore:

\[
\boxed{
\text{the one-variable collision strata contain only the removed source-product diagonal.}
}
\tag{4.2}
\]

The only surviving collision stratum has

\[
d\ne d',\qquad m\ne m',\qquad p\mid dm-d'm'.
\tag{4.3}
\]

For distinct products below `H`, at most one first-band prime can satisfy (4.1).

## 5. Exact weighted multiplicative energy

For coefficients `alpha_d`, `gamma_m` supported on units modulo `p`, put

\[
r_p(a)=\sum_{dm\equiv a\pmod p}\alpha_d\gamma_m,
\qquad
A=\sum_d\alpha_d,
\qquad
C=\sum_m\gamma_m.
\]

Then multiplicative-character orthogonality gives

\[
\boxed{
\sum_{a\in\mathbb F_p^\times}
\left|r_p(a)-\frac{AC}{p-1}\right|^2
=
\frac1{p-1}
\sum_{\chi\ne\chi_0}
\left|\sum_d\alpha_d\chi(d)\right|^2
\left|\sum_m\gamma_m\chi(m)\right|^2.
}
\tag{5.1}
\]

For unweighted intervals, the Ayyad--Cochrane--Zheng multiplicative-congruence theorem yields the expected diagonal plus an `UV p^{o(1)}` error.  It therefore explains the full factor-`p` saving available when the principal product distribution cancels.

The Fortune cells are not unweighted intervals.  Their first factor is `mu(d)`, and the second factor is the signed coefficient generated by (1.1).  The required fixed-modulus statement is the actual-coefficient estimate

\[
\boxed{
\frac1{p-1}
\sum_{\chi\ne\chi_0}
|M_D(\chi)|^2|C_M(\chi)|^2
\ll
\|\mu\|_{2,D}^2\|c\|_{2,M}^2 X^{o(1)},
}
\tag{5.2}
\]

where

\[
M_D(\chi)=\sum_{d\sim D}\mu(d)\chi(d),
\qquad
C_M(\chi)=\sum_{m\sim M}c(m)\chi(m).
\]

A generic coefficient estimate cannot prove (5.2): a coefficient may imitate one character.  The arithmetic Mobius and Heath--Brown structure is essential.

## 6. Fixed modulus is not the complete theorem

Even (5.2) for every physical prime would control only

\[
\sum_{j,p}|\Delta_{j,p}|^2.
\]

The full-band target contains the coherent square over the physical primes and every higher survivor order.  Cross terms with two distinct physical primes have combined modulus

\[
c=ps\asymp X^2\asymp H,
\]

while the critical bilinear variables have length `asymp X=sqrt c`.

The new Blomer--Pascadi bilinear Kloosterman theorem, arXiv:2607.24311, gives a saving `c^{-1/32}` over the trivial estimate in this square-root range.  A literal insertion at the unrecombined cross-conductor gate therefore saves only `X^{-1/16}`.  The preceding exact scale ledger requires the full missing factor `X^{1-o(1)}/log X` before the physical and high-order terms are separated.  The theorem is a genuine improvement in Kloosterman technology but is not a black-box finish for this source.

This comparison does not exclude a more efficient derivation in which the full survivor recombination removes most of the cross-conductor main term before a Kloosterman estimate is used.  No such identity has been proved.

## 7. Exact remaining theorem

The surviving first-band theorem is the punctured-Mobius specialization of `FBPOTD(X)`:

\[
\boxed{
\sum_{j\in B}
\left|
\sum_{d\le Y}\mu(d)
\sum_m c_d(m)
\,g_R\!\left(-m(P_j/d)^{-1}\right)
\right|^2
\ll
\sum_{j,n}|b(n)|^2|g_R(-nP_j^{-1})|^2
+E_{B,R},
}
\tag{7.1}
\]

with:

1. `c_d` exactly as in the reduced source identity;
2. the integer product diagonal removed before estimation;
3. the low collision kernel fixed by (4.3);
4. all conductors of order at least two retained with their signs;
5. errors summable over cells, bands and centre blocks.

After (2.1)--(4.3), the irreducible analytic content is:

\[
\boxed{
\text{Mobius-weighted multiplicative energy plus coherent cross-conductor survivor cancellation.}
}
\tag{7.2}
\]

## 8. Verification

The committed verifier checks exactly:

- the full-band amplitude square split;
- the deterministic source diagonal formula;
- punctured-centre transport;
- the punctured-centre Gram collision;
- disappearance of the one-variable collision strata;
- the determinant kernel `p|dm-d'm'`;
- at-most-one collision prime for distinct products;
- the weighted residue-energy identity.

Complete panels: `X=11,17,23`.

## 9. Boundary

**PROVED EXACTLY**

- complete source-product diagonal formula;
- Fortune admissibility of the diagonal;
- punctured-centre Gram;
- one-variable collision collapse;
- generic determinant kernel;
- weighted multiplicative residue-energy identity.

**SUPPORTED BY PUBLISHED INPUT**

- the unweighted interval analogue of the fixed-modulus energy has the expected main term and `UV p^{o(1)}` error.

**CLOSED AS BLACK-BOX ROUTES**

- another generic punctured-centre frame followed by Cauchy;
- unweighted multiplicative energy as a substitute for the actual Mobius-weighted theorem;
- direct use of the current square-root Kloosterman saving at the unrecombined cross-conductor scale;
- separate positive physical and high-order estimates.

**OPEN**

- (5.2) uniformly for every actual source cell;
- coherent physical/high-order full-band contraction (7.1);
- signed source-cell recombination at theorem scale;
- cross-band `NSMT(X)` and Fortune's conjecture.
