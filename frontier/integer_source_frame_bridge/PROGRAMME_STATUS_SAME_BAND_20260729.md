# Integer Fortune programme status after the same-band audit

Date: 29 July 2026

## Current status

Fortune's conjecture is **OPEN**.

The rough-quotient collapse and same-band recombination are now exact with the correct frozen cutoff and endpoint conventions. CI covers the orbit verifier, the generalized rough-quotient verifier and an exact same-band decomposition verifier.

## New exact results

1. general-cutoff quotient transport for `Z=z_B\ge z_j`;
2. exact Möbius-floor and sawtooth formula at the frozen cutoff;
3. exact half-open dyadic partition with no missing first band;
4. exact same-band `TT^*` covariance identity;
5. exact rejection of physical `q\leftrightarrow k` symmetry;
6. exact rejection of direct reuse of the existing Ramanujan projector;
7. exact identification of the signed truncation/cross-modulus theorem required.

## Current obstruction

The missing theorem is a signed common-source cross-modulus dispersion bound for primorial-rough quotient intervals of length `H/q\le X`. Standard operator tools only reformulate the covariance; after absolute values they lose either `R/\log R` in the modulus band or `2^{\pi(X)}` in the smooth-divisor sum.

## Phase D

Not executed as a theorem. The physical first-order estimate is still open, so the normalized rough coordinate and ordered Buchstab tail have not been claimed as reinserted. Earlier reinsertion remains a viable alternative research path because separate chaos-level control is sufficient but not necessary.

## Labels

- **PROVED EXACTLY:** corrected reductions and identities listed above.
- **COMPUTATIONALLY VERIFIED:** finite panels and dyadic decomposition.
- **EMPIRICAL:** bounded finite same-band ratios.
- **CONDITIONAL:** physical first-order finish from a uniform same-band theorem.
- **OPEN:** same-band theorem, full variance theorem and Fortune's conjecture.
- **RETRACTED:** the unqualified `Z=z` formulation as the mesoscopic frozen theorem; it is replaced by the general-cutoff version.
