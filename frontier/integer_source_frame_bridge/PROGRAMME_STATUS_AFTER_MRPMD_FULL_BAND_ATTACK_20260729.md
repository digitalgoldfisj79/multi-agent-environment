# Programme status after the autonomous `MRPMD` / full-band attack

Date: 29 July 2026  
Branch: `gpt56/fortune-mesoscopic-cotlar-20260728`

Status: the corrected same-band obstruction has been pursued through both remaining routes: direct cross-modulus dispersion and complete normalized-survivor reinsertion. The first physical band is now exactly a primorial-shift weighted Titchmarsh divisor problem. Recombining all Euler orders sends every order at least two into a one-point semiprime/higher shrinking-target problem. Current large-modulus theorems do not match the prime-modulus and exponentially large-shift geometry, while pair injectivity alone is polynomially too weak. Both routes therefore meet at one genuine theorem-level obstruction. Fortune's conjecture remains **OPEN**.

## 1. Exact first-order compression

With

\[
\omega_R^\ast(n)
=
\sum_{\substack{p\in\mathcal P_R\\p\mid n}}
\frac{p-1}{p-2},
\qquad
\lambda_R=\sum_{p\in\mathcal P_R}\frac1{p-2},
\]

the frozen same-band coordinate is exactly

\[
\frac{T_{B,R}(j)}{\beta_j}
=
\sum_{m\in\mathcal M_Z}\omega_R^\ast(P_j+m)
-
(M_Z-1)\lambda_R.
\]

Thus `MRPMD(X)` / `SBD(X)` is a truncated Titchmarsh variance theorem with prime variable `m\le H\asymp X^2` and primorial shift `P_j=\exp((1+o(1))X)`.

## 2. Exact full-band reinsertion

The complete normalized-survivor band is

\[
\mathcal U_{j,R}
=
\beta_j\left[V_R^{-1}(M_Z-H_{j,R})-M_Z\right],
\]

where `H_{j,R}` is the number of sources hit by at least one first-band prime.

For every product `Q` of at least two first-band primes,

\[
Q>H.
\]

Hence the intersection source is the single candidate

\[
\rho_{j,Z}(Q)
=
Z+1+\bigl(-P_j-(Z+1)\bmod Q\bigr),
\]

and the intersection contributes exactly when this candidate lies below `H` and is prime. Every Euler order at least two is therefore a one-point shrinking-target roughness test.

## 3. Positive reinsertion does not close

Pair injectivity gives

\[
\sum_m\binom{h_{j,R}(m)}2
\le
\binom{|\mathcal P_R|}{2}.
\]

At first-band scale, an argument using only this information permits a correction of size

\[
\beta_j|\mathcal P_R|^2
\asymp
\frac{X^2}{\log X},
\]

whose square is polynomially above the Fortune row scale. Pair injectivity identifies the geometry but is not an estimate at the required strength.

Finite exact panels also show that the covariance between first order and the all-order correction changes sign. Triangle-inequality reinsertion is therefore not justified.

## 4. Literature gate

- Uniform residue-class results beyond the square-root barrier require moduli with factors in prescribed ranges. The physical moduli here are prime `p\asymp H^{1/2}`.
- Uniform Titchmarsh divisor results allow long shifts only when the shift is comparable to the prime-variable range. Here `P_j` exceeds every fixed power of `H`.
- At order two, the modulus already exceeds `H` and each progression has at most one candidate.

No current result found supplies the required deterministic primorial-shift sampling theorem.

## 5. Boundary

**PROVED EXACTLY**

- weighted primorial-shift Titchmarsh compression;
- first-order/self drift identity;
- complete union-hit survivor formula;
- explicit higher-order correction;
- semiprime/higher one-point transition;
- inclusion-exclusion interface;
- pair injectivity.

**PROVED FROM CLASSICAL INPUT**

- the pair-injectivity-only scale is insufficient for a Fortune bound.

**COMPUTATIONALLY VERIFIED**

- every identity on complete panels `X=11,17,23,29,37`;
- sign-indefinite first-order/higher-order covariance.

**CLOSED AS DIRECT ROUTES**

- factorable-modulus large-prime progression theorems as black boxes;
- existing uniform Titchmarsh theorems as black boxes;
- pair-injectivity-only positive control;
- separate first-order/higher-order triangle bounds.

**OPEN**

- `MRPMD(X)` / `SBD(X)`;
- signed semiprime/higher shrinking-target sampling;
- complete `NSMT(X)`;
- the Fortune variance theorem and Fortune's conjecture.

The decisive unresolved theorem is now:

\[
\boxed{
\text{signed deterministic sampling of primorial-shift roughness across physical and one-point conductors}.
}
\]

Authoritative note:

- `frontier/integer_source_frame_bridge/PRIMORIAL_SHIFT_TITCHMARSH_AND_FULL_BAND_INTERFACE_20260729.md`
