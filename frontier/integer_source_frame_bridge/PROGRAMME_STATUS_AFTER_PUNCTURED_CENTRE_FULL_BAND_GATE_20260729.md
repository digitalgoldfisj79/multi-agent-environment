# Programme status after the punctured-centre full-band gate

Date: 29 July 2026  
Branch: `gpt56/fortune-mesoscopic-cotlar-20260728`

## Current status

The full-band programme has been run through the exact prime-source, source-diagonal, low-mode collision and direct analytic-technology gates.  It produced a stronger source identity, removed the provisional small-variable overlap problem exactly, proved that the complete source-product diagonal is Fortune-admissible, and reduced the surviving low-mode arithmetic to one explicit Mobius-weighted multiplicative determinant kernel.  The first physical band, `NSMT(X)` and Fortune's conjecture remain **OPEN**.

## 1. Gate A -- passed in stronger form

With

\[
Y=\lceil\sqrt H\rceil<X,
\]

the exact two-level Heath--Brown identity is

\[
\Lambda
=
2\mu_{\le Y}*\log
-
\mu_{\le Y}*\mu_{\le Y}*1*\log
\qquad(n\le H).
\]

It resums exactly to

\[
\boxed{
\Lambda
=
\mu_{\le Y}*\log
+
\mu_{\le Y}*\mu_{>Y}*1*\log.
}
\]

Thus there is only one small Mobius variable.  Every nonzero small variable `d` is squarefree, satisfies `d|P_j`, and obeys

\[
\boxed{
p\mid P_j+dm
\iff
p\mid P_j/d+m.
}
\]

The provisional three-small-variable identity and all separate overlap classes are closed by exact algebra.

## 2. Gate B -- passed

For the fully recombined prime source, the complete survivor coordinate is `delta_R=V_R^{-1}-1` on a survivor and `-1` on a hit.  The exact source-product diagonal is

\[
\boxed{
\mathcal D_{j,R}
=(M_Z-H_{j,R})\delta_R^2+H_{j,R}.
}
\]

Consequently

\[
\sum_{j\in B}\beta_j^2\mathcal D_{j,R}
\ll KH\log X,
\]

which is below the Fortune block allowance `KHX/log X` by a polynomial factor.  The sparse prime-power correction is smaller.  The full-band route does not fail on its diagonal.

## 3. Gates C and D -- exact reduction completed

The punctured-centre fixed-modulus Gram is

\[
\boxed{
G_p((j,d),(k,d'))
=
\frac1p\mathbf1_{p\mid P_jd'-P_kd}-\frac1{p^2}.
}
\]

For balanced source products `dm,d'm'<p^2`, after the integer product diagonal has been removed, the low collision condition is

\[
\boxed{p\mid dm-d'm'.}
\]

If `d=d'`, collision forces `m=m'`; if `m=m'`, collision forces `d=d'`.  Both one-variable strata therefore consist only of the already-removed product diagonal.  The sole surviving collision stratum has

\[
d\ne d',\qquad m\ne m',\qquad p\mid dm-d'm'.
\]

At most one first-band prime can divide the nonzero determinant `dm-d'm'`.

## 4. Exact fixed-modulus energy

For

\[
r_p(a)=\sum_{dm\equiv a\pmod p}\alpha_d\gamma_m,
\]

one has

\[
\boxed{
\sum_{a\in\mathbb F_p^\times}
\left|r_p(a)-\frac{AC}{p-1}\right|^2
=
\frac1{p-1}
\sum_{\chi\ne\chi_0}
\left|\sum_d\alpha_d\chi(d)\right|^2
\left|\sum_m\gamma_m\chi(m)\right|^2.
}
\]

For the Fortune source, `alpha_d=mu(d)` and `gamma_m` is the exact coefficient generated by the reduced prime-source identity.  Unweighted interval multiplicative-energy theorems do not imply the required signed estimate for these coefficients.

## 5. Analytic gate -- partial, with one explicit theorem left

The applicable direct black boxes were tested against the literal parameters.

1. Prime-modulus large-sieve input controls positive character energy.  At the complete source length `N asymp H asymp X^2` and prime-modulus scale `Q asymp X`, its parameter `Q/sqrt N` is only constant, so it does not provide the missing logarithmic or polynomial cross-modulus contraction.
2. Unweighted multiplicative-congruence estimates supply the expected fixed-modulus scale for intervals, but not the Mobius/Heath--Brown weighted source.
3. Current square-root bilinear Kloosterman estimates provide a genuine power saving on an individual combined modulus `ps asymp X^2`, but a direct insertion before full survivor recombination is smaller than the `X^{1-o(1)}/log X` saving required by the branch scale ledger.
4. Separate positive treatment of the physical and high-conductor terms remains invalid because their covariance is sign-indefinite.

The remaining first-band theorem is therefore

\[
\boxed{
\sum_{j\in B}
\left|
\sum_{d\le Y}\mu(d)
\sum_m c_d(m)
\,g_R\!\left(-m(P_j/d)^{-1}\right)
\right|^2
\ll
\sum_{j,n}|b(n)|^2|g_R(-nP_j^{-1})|^2
+E_{B,R},
}
\]

with the exact source coefficients, the product diagonal removed, and all physical and higher conductor orders retained with their signs.

Its irreducible arithmetic content is

\[
\boxed{
\text{Mobius-weighted multiplicative energy plus coherent cross-conductor survivor cancellation.}
}
\]

## 6. Execution verdict

### PROVED EXACTLY

- the two-level Heath--Brown identity on the complete source range;
- the one-small-variable resummation;
- exact punctured-centre transport;
- the complete source-product diagonal formula and its Fortune admissibility;
- the punctured-centre Gram;
- collapse of the one-variable collision strata;
- the determinant kernel `p|dm-d'm'`;
- the weighted multiplicative residue-energy identity.

### COMPUTATIONALLY VERIFIED EXACTLY

- the source identities on complete panels `X=11,17,23,29,37`;
- punctured-centre and Gram identities;
- source diagonal/off-diagonal decomposition;
- one-variable collision collapse;
- generic determinant checks on panels `X=11,17,23`.

### CLOSED AS DIRECT ROUTES

- the provisional three-small-variable source decomposition;
- separate small-variable overlap estimates;
- another generic frame followed by Cauchy;
- unweighted multiplicative energy as a substitute for the actual source;
- direct unrecombined insertion of present square-root Kloosterman bounds;
- separate positive physical/high-conductor estimates.

### OPEN

- the actual-coefficient Mobius-weighted fourth-moment estimate;
- coherent physical/high-conductor full-band contraction;
- signed source-cell recombination at theorem scale;
- the first physical-band theorem, `NSMT(X)`, the Fortune variance theorem and Fortune's conjecture.

## Authoritative notes

- `PRIME_SOURCE_TWO_LEVEL_IDENTITY_AND_PUNCTURED_CENTRE_20260729.md`
- `PUNCTURED_CENTRE_FULL_BAND_GATE_20260729.md`
