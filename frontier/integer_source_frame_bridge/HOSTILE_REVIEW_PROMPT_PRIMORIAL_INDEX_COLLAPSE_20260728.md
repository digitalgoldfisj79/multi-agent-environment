# Hostile review prompt: primorial-index collapse

Review the following theorem package adversarially:

1. `PRIMORIAL_INDEX_SHRINKING_TARGET_COLLAPSE_20260728.md`;
2. `LARGEST_FACTOR_ROUTED_BESSEL_INEQUALITY_20260728.md`;
3. `LARGEST_FACTOR_HYPERBOLA_SOURCE_IDENTITY_20260728.md`;
4. `PROGRAMME_STATUS_AFTER_PRIMORIAL_INDEX_COLLAPSE_20260728.md`.

Check every implication, with particular attention to:

- the least-positive-residue convention;
- whether `d>H Q_{j,k}` really forces equality in the congruence;
- the use of `m>X`, `Q>=X`, and `H=eta X^2` with `eta<1`;
- the conversion from pair separation to visit multiplicity;
- the claim that a largest factor in an `R`-factor product is at least
  `(P_j+m)^(1/R)`;
- the asymptotic `O(R)` centre-multiplicity conclusion;
- grouping `Lambda=mu*log` by unordered complementary divisor pairs;
- the diagonal `D=E` weight;
- the one-point formula using `E_j(D)=ceil(P_j/D)`;
- the condition `sqrt(P0)>H`;
- whether bounded column multiplicity is being overinterpreted as a complete
  variance bound;
- every statement retracting the former growing-depth obstruction.

Distinguish:

1. exact theorem errors;
2. omitted hypotheses or finite-threshold qualifications;
3. valid identities with overstrong strategic interpretation;
4. genuinely open signed estimates.

Do not criticise the package merely because Fortune's conjecture remains open.
Quote the precise statement for every adverse finding.  End with the strongest
corrected theorem boundary that survives.
