# Corrected integer Fortune programme: authoritative status

Date: 27 July 2026  
Branch: `gpt56/fortune-mainterm-correction-programme-20260727`

## Completed

1. **Main-term correction accepted.** Below the square threshold the Fortune detector is a prime-pair detector, not an ordinary shifted-prime detector.
2. **Correct all-centres criteria proved.** Unweighted, output-weighted and double-von-Mangoldt versions are recorded with their correct scales.
3. **Finite calibration completed.** Pointwise and full dyadic-block computations support the singular-series calibration at accessible sizes.
4. **Exact Fourier source identity proved.** The first deterministic path kernel is the single-walk exponential sum.
5. **Exact two-sided signed divisor frame proved.** The CRT solvability projector is `(d,e)|P_j`; independent finite verification passed.
6. **Output-side sieve blindness proved.** Candidate outputs are automatically coprime to the full primorial, so a small-prime output sieve cannot distinguish prime from composite candidates.
7. **Papers II--III reopened honestly.** Their first-circulation binaries are retained as provenance, while the affected arithmetic sections are explicitly superseded pending corrected editions and fresh review.

## Surviving integer route

The corrected source must remain asymmetric:

- expose or retain the prime condition on the offset, whose scale is `X^2`;
- retain the output von Mangoldt function at its exponential centre;
- subtract the primorial prime-pair singular-series main term before absolute values;
- prove a sparse-centre block first moment and then an aggregated four-prime covariance estimate.

The first positive landmark is

\[
\sum_{j<N}Z_j(H)\sim\sum_{j<N}\lambda_j(H).
\]

The all-centres theorem remains

\[
\sum_{j<N}|Z_j(H)-\lambda_j(H)|^2
\ll NX L(X),
\qquad L(X)=o(\log X).
\]

## Deferred work

The following are not on the critical path until a corrected bridge is proved:

- HTE4 and its median-channel refinements;
- the pair-sum exceptional-set theorem;
- Paper IV derandomisation;
- further Lebesgue moment calculations;
- symmetric two-sided polynomial-scale divisor truncation.

## Current theorem-level obstruction

No existing theorem controls a prime-pair correlation at the exponentially sparse primorial centres with the required block accuracy. The exact missing input is now one of:

1. a hybrid sparse-centre prime-pair first-moment theorem;
2. an aggregated four-linear-form covariance theorem;
3. a signed transformation of either quantity to a tractable deterministic kernel without discarding the offset-prime condition.

This is a genuinely new analytic-number-theory requirement. The programme has been corrected and narrowed, but it has not produced a proof of Fortune's conjecture.

Papers V--VI remain a separate function-field programme and are unaffected by this correction.
